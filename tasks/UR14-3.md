# UR14-3 — Panel polish batch (UI-REVIEW.md items 2, 8, 9, 12, 13, 20-23 + Ray's overlap bug)

Read UI-REVIEW.md + Ray's screenshot research/ray-webpage-overlap-bug.png
(Web Page section: labels OVERLAP their inputs — genuine CSS bug, fix first).
1. **Fix the Web Page section layout** (the screenshot bug) — if
   src/ui/propRow.css exists (UR16-2's shared row grid), ADOPT it for the
   Properties Object tab sections (Web Page, transforms, etc.); else fix with
   equivalent scoped CSS and note it. Label the ▶/●/Re-rasterize buttons with
   text/tooltips while there.
2. **Instant styled tooltips** (2): shared tooltip module (~150ms, name +
   shortcut) for toolbar buttons, panel-header :: and ⋮ glyphs (13), topbar
   icon buttons.
3. **Properties header shows current tab** (8): "Properties · Material".
4. **Collapsible sections in long tabs** (9): Object + Material(+Super) tabs,
   persisted like shadePrefs.sections (a small ui-prefs store is fine).
5. **N-panel tab hit targets ≥ 24px** (12).
6. **Shading rows: drop the double bullet** (20); **label contrast** to
   ~#b8b8b8 (21); **weight-based type hierarchy** + eyebrow letterspacing
   (22); **timeline interp/easing gated on a keyed selection** (23).
STRICT scope: CSS + the named UI files. materialTab is owned by another
chain — if UR16-2's new tab has landed, apply only the generic polish items
(tooltips/contrast) THROUGH ITS OWN css file without restructuring; if not
landed, skip materialTab entirely and note it.
Acceptance: tsc, units, affected e2e green + new checks: Web Page labels no
longer overlap (rect intersection probe on that section with an html plane
selected — Ray's exact repro); tooltip appears on toolbar hover; properties
header names the tab; N-panel tab rects ≥24px. Screenshots incl. the FIXED
Web Page section, LOOKED at. Free-text report, FIRST LINE STATUS:.
