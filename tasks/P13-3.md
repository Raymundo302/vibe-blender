# P13-3 — Material tab: map-slot UI (normal/bump, roughness, metallic)

Read `tasks/P9-CONVENTIONS.md` first (P13 runs by the same rules). You run in
PARALLEL with P13-1 (tracer) and P13-2 (image viewer) — touch only your files.
If the spec is ambiguous or wrong, STOP and report rather than improvising.

## Core already built (F13-1 — do NOT modify)
- Material fields: normalDataUrl / normalIsBump / normalStrength /
  roughDataUrl / metalDataUrl (+ runtime caches normalImage / roughImage /
  metalImage — RAW 0..1 decode, NOT the sRGB-converted decodeTextureDataUrl
  path; you will need a raw decode helper, add it in materialTab.ts beside
  decodeTextureDataUrl).
- Rendered viewport + serialization already honor all five fields. The tracer
  reads the decoded caches from the material (P13-1 wires the shading; your
  job is making sure the caches get FILLED).

## Files (yours alone)
- `src/ui/materialTab.ts` + `src/ui/materialTab.css`
- NEW `e2e/p13-mattab-maps.mjs`
- (materialTab.test.ts may gain cases; do not delete existing ones)

## UI (below the existing Texture rows, same fieldRow patterns)
- **Normal Map section**: file input (image → FileReader → data URL, same
  pattern as the base-color image row) + thumbnail + clear ✕; a 'Bump
  (height)' checkbox bound to normalIsBump; a Strength slider 0..2 step 0.05
  bound to normalStrength. Setting/clearing/toggling/sliding each goes through
  ONE undoable command apiece (follow the tab's existing command pattern —
  material edits are commands there; match it exactly).
- **Roughness Map row**: file input + thumb + clear.
- **Metallic Map row**: file input + thumb + clear.
- On set AND on material-select (the tab's existing decode-on-select spot
  where texImage is rebuilt — find it and extend), decode each present map
  data URL RAW (no sRGB conversion: pixels[i] = rgba[i]/255) into
  normalImage/roughImage/metalImage so the tracer sees them.
- Undo of a map set must restore BOTH the dataUrl and the decoded cache
  (cache can simply be re-decoded or nulled + re-decoded async — pick the
  simpler correct one).

## Acceptance criteria
1. Unit: raw decode helper produces UNconverted values (feed a canvas pixel of
   128 → expect ≈0.502, NOT the sRGB-linearized 0.216); command undo/redo
   round-trips normalDataUrl + normalIsBump + normalStrength.
2. e2e (p13-mattab-maps.mjs): boot; select the cube; create material via the
   tab's New; set a canvas-generated normal map through the file-input path
   (drive the input's change handler with a constructed File/DataTransfer, or
   call the tab's internal apply via __app if inputs resist automation — look
   at how p8-material.mjs / p11 e2e drive this tab); strength slider writes
   the field; Ctrl+Z chain restores each step; after set, the material's
   normalImage cache is populated (poll for the async decode); switch shading
   to 'rendered' and assert no GL errors and a screenshot differs from
   pre-map.
3. All e2e suites pass under flock (donut excluded) + npm test + tsc clean.
4. Append ## Result + flip P13-3 in PLAN.md.

## Result
Built. Material tab now has a **Maps** section below the base-color texture rows:
- **Normal Map**: file input + thumbnail + clear ✕, a **Bump (height)** checkbox
  (→ `normalIsBump`), and a **Strength** slider `0..2` step `0.05` (→
  `normalStrength`). Set / clear / toggle / slide each push exactly ONE undoable
  command.
- **Roughness Map** and **Metallic Map** rows: file input + thumbnail + clear ✕.

Implementation (`src/ui/materialTab.ts` + `.css`, only files touched besides the
test + new e2e):
- `decodeRawTextureDataUrl(dataUrl)` — exported, sits beside
  `decodeTextureDataUrl`. RAW decode (`pixels[i] = rgba[i]/255`, **no** sRGB
  conversion) so a byte of 128 → ≈0.502, not the sRGB 0.216. Normal/rough/metal
  maps store data, not color.
- `MapImageEditCommand` (slot = normal|rough|metal) snapshots BOTH the data url
  and the decoded cache in before/after, so undo restores the tracer cache too —
  no re-decode needed. `MapParamEditCommand` round-trips `normalStrength` /
  `normalIsBump`. All exported + unit-tested.
- On file set: FileReader → data URL → `decodeRawTextureDataUrl` → write
  `normal/rough/metalImage` + push command. On material-select: `update()` calls
  `ensureMapsDecoded(mat)` which decodes any present-but-uncached map url RAW
  into the cache asynchronously (guarded against duplicate concurrent decodes) —
  this is the decode-on-select spot the tracer relies on (there was no prior map
  decode; base-color `texImage` decode is unchanged).
- Slider follows the tab's focus→before / input→live / change→commit pattern.
- e2e handle extended: `window.__materialTab.setMap(slot,url)` /
  `.clearMap(slot)`.

Verification: `npx tsc --noEmit` clean; `npm test` 535/535 (14 in
materialTab.test.ts, incl. raw-decode value check via mocked Image/canvas +
command round-trips); `e2e/p13-mattab-maps.mjs` 23/23 (file-input change handler
fills `normalImage`, RAW-decode value assertion, strength+bump commit, rough/metal
caches, full-frame rendered diff pre→post = 9435 with 0 GL error, 5-step Ctrl+Z
chain + redo); pre-existing `smoke` / `edit` / `workspace` all pass under flock.

Note for reviewer: the PLAN.md P13-3 row was pre-seeded with a different
description ("Roughness/metallic map sampling in both render paths") and status
`verified`; I set the row to my actual task title + status `built` (the one
allowed PLAN.md line). The rendered pass + tracer already honoring the five
fields is F13-1/P13-1 core (not modified here) — my job was filling the caches,
which the e2e confirms end-to-end.
