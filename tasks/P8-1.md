# P8-1 — Light properties tab

Read `tasks/P8-CONVENTIONS.md` first.

## Goal
A "Light" properties tab (💡) that edits the ACTIVE object's LightData live.
Lights already exist end-to-end (Shift+A → Add Light, viewport icon,
click-select, rendered mode uses them) — this task is ONLY the data UI.

## Files (yours alone)
- `src/ui/lightTab.ts` — fill the stub (registerPropertiesTab; it is already
  imported by main.ts)
- NEW unit test file next to it for any pure helpers you extract
- NEW `e2e/p8-light.mjs` — your own suite (harness pattern from ship.mjs)
- NEW `src/ui/lightTab.css` — imported from lightTab.ts, `light-tab-` prefix

## Behavior
- Tab shows an empty state ("No light selected") unless the active object's
  kind === 'light' (follow modifierTab's empty-state pattern).
- Fields: type select (Point/Sun/Spot), color (`<input type="color">`,
  hex ↔ linear 0..1 conversion — just treat hex as linear like obj.color
  elsewhere), power (number, min 0), and for spot only: angle (degrees in the
  UI, RADIANS in LightData) + blend (0..1). Spot fields hidden for other types.
- Changing type keeps color/power (defaultLight is only for NEW lights).
- Every commit (change event) pushes ONE undoable command that restores the
  full previous LightData on undo. Follow the modifierTab capture pattern.
- Live: the rendered viewport must reflect edits immediately (it already reads
  scene state per frame — you just mutate obj.light via your command).

## Acceptance criteria
1. Unit: hex↔rgb round trip; command undo restores the exact prior LightData.
2. e2e (p8-light.mjs): via __app add a light (`scene.addLight('Point','point')`,
   selectOnly), switch Properties to the Light tab, assert fields present and
   power edit + Ctrl+Z round-trips; switch type to spot → angle/blend appear.
3. All e2e suites + npm test + npm run build pass.

## Result
Built — status: built.

Files (mine alone):
- `src/ui/lightTab.ts` — filled the stub. Registers a `light` properties tab
  (💡, tooltip 'Light') via `registerPropertiesTab`. Shows a "No light selected"
  empty state unless `scene.activeObject.kind === 'light'`. Fields: type select
  (Point/Sun/Spot), color (`<input type="color">`, hex↔linear rgb like obj.color),
  power (number, clamped ≥0), and spot-only Angle (degrees in UI ↔ radians in
  LightData) + Blend (0..1 clamped). Spot block hidden via the `hidden` attribute
  for point/sun. Type change keeps color/power/cone (defaultLight untouched).
  Follows the Object/Modifier tab conventions: skip field rewrites while a field
  is focused, refresh on commit. Exports pure helpers `rgbToHex`, `hexToRgb`,
  `cloneLight`, and the `LightCommand` undo command (snapshots full LightData
  before/after; assigns fresh clones on undo/redo so the live object never shares
  a reference with the snapshots).
- `src/ui/lightTab.css` — `light-tab-` prefixed styles for rows/inputs.
- `src/ui/lightTab.test.ts` — 6 unit tests: hex↔rgb round-trip + clamping,
  cloneLight deep copy, LightCommand undo/redo restores exact prior LightData,
  and snapshot independence from later live mutations.
- `e2e/p8-light.mjs` — adds a Point light via `__app`, switches to the Light tab,
  asserts fields present, power edit + Ctrl+Z/Ctrl+Shift+Z round-trip, color edit,
  type→spot reveals angle/blend (degrees↔radians), and mesh-active empty state.

Verification (all green):
- `npx tsc --noEmit` clean.
- `npm test` — 251 unit tests pass (incl. 6 new).
- `npm run build` — succeeds.
- e2e: `p8-light` (17 checks) + existing `smoke`, `edit`, `ship`, `workspace`
  all pass under `flock /tmp/vibe-blender-e2e.lock`.

No shared files touched (main.ts already imports the stub; nothing else changed).
