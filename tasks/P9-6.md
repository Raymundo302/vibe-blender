# P9-6 — Selection & viewport UX: loop select, X-ray, recalc normals, crease key, camera-to-view

Read `tasks/P9-CONVENTIONS.md` first. Runs AFTER batch A — P9-1..P9-5 are in
the tree; do not touch their files. You are the ONLY worker in this slot that
may edit InputManager.

## Files (yours alone)
- `src/input/InputManager.ts` — Alt+click, Alt+Z, Shift+N, Shift+E, Ctrl+Alt+Numpad0
- NEW `src/core/mesh/ops/loopSelect.ts` (+ NEW test)
- NEW `src/core/mesh/ops/recalcNormals.ts` (+ NEW test)
- NEW `src/tools/creaseOp.ts` (+ test if you extract pure math)
- `src/render/passes/elementPickPass.ts` + `src/tools/boxSelect.ts` — x-ray plumbing
- `src/ui/helpOverlay.ts` — new shortcut rows
- `src/ui/topbar.ts` — an X-ray toggle chip (mirrors Alt+Z state)
- NEW `e2e/p9-select.mjs`

## Alt+click loop select (edit mode)
- Edge mode: Alt+click an edge → select the full edge loop (walk: at each
  4-valence vert continue with the edge "across" — not sharing a face with the
  incoming edge; stop at boundaries/poles or on closing the loop). Shift+Alt+
  click adds a second loop to the selection.
- Face mode: Alt+click a face → face loop through quads (enter via the edge
  under the click: use the picked face + nearest edge; document your choice).
- Vertex mode: like edge mode, selecting the loop's verts.
- Pure walk logic in loopSelect.ts with unit tests on grid/torus meshes
  (makeTorus is parametric now).

## X-ray select-through (Alt+Z + topbar chip)
When ON: element picking and box select must hit BACK-side elements too.
Implementation guidance (verify feasibility, adjust honestly): elementPickPass
renders element ids with depth — for x-ray, render WITHOUT the mesh-occlusion
prepass / with depth test disabled so back elements win where front ones are
absent; box select in x-ray mode selects every element whose projection falls
in the rect regardless of depth (the CPU projection path in boxSelect can just
skip its occlusion check if one exists — read the real code first). Blender
also draws the mesh translucent in x-ray; a cheap alpha hint in the solid pass
is optional polish, not required.

## Shift+N recalculate normals (edit mode)
recalcNormals(mesh, faceIds): flood-fill winding consistency across shared
edges (BFS from the largest-area face of each island; neighbors sharing an
edge traversed in the SAME direction get flipped), then orient each island
outward (positive signed volume vs island centroid; flip all if negative).
Undoable (MeshEditCommand.capture). Status "Recalculated normals".

## Shift+E crease (edit mode, edge selection)
Modal operator like the existing transforms: horizontal pointer motion (or
typed numeric 0..1, or -1 to clear) sets the crease weight on the selected
edges via mesh.setCrease; live preview (creased subsurf updates each frame);
LMB/Enter confirm, RMB/Esc cancel+restore, undoable. Show weight in status.
Creased edges draw highlighted in the edit cage IF cheap (editOverlayPass is
NOT yours — skip visual flair rather than touching it; say so in Result).

## Ctrl+Alt+Numpad0 — snap active camera to current view
Sets the active camera's transform from the current OrbitCamera pose
(position = eye, rotation = look direction via quaternion from the view
basis; write a pure helper + unit test round-tripping through cameraViewMatrix
from P8-2). Undoable (transform command pattern exists in nPanel/properties).
Status 'Camera set to view'. If no camera exists: create one via
scene.addCamera + AddObjectsCommand, then snap it (Blender-ish, document).

## Acceptance criteria
1. Unit: loop walk on a 4×4 grid picks the straight loop and stops at
   boundaries; on a torus it closes (count = segment count); recalcNormals
   fixes one deliberately flipped face on a cube (all normals outward:
   signed volume positive, every faceNormal·(centroid-center) > 0);
   camera-to-view helper round-trips.
2. e2e: cube → edit → edge mode → Alt+click selects 4-edge loop (assert
   selection count via __app); x-ray ON box-select over the whole cube in
   vertex mode selects all 8 verts, OFF selects only front 4-ish (assert
   more-vs-fewer); Shift+E drag sets crease > 0 on selected edges; Ctrl+Z
   restores; Ctrl+Alt+Numpad0 moves the camera (transform changed, undoable).
3. All e2e suites (flock) + npm test + npx tsc --noEmit pass.

## Result
DONE. tsc clean, 369 unit tests green (+13 new), e2e/p9-select.mjs green (21 checks)
plus all existing suites (smoke, edit, ship, workspace, p8-*, p9-*).

Delivered:
- **Alt+click loop select** — pure walks in `src/core/mesh/ops/loopSelect.ts`
  (`edgeLoop`/`vertLoop`/`faceLoop`), unit-tested on a 4×4 grid (straight loop,
  stops at boundaries) and a torus (closes at segment count). InputManager
  `loopSelectAt`: edge/vert modes ride the edge nearest the cursor (vert mode
  selects that loop's verts); face mode picks the face + its edge whose
  projected midpoint is nearest the cursor (documented entry-edge choice).
  Plain Alt replaces, Shift+Alt adds a loop.
- **Shift+N recalculate normals** — `src/core/mesh/ops/recalcNormals.ts`: BFS
  from each island's largest-area face for winding consistency, then
  signed-volume flip for outward orientation. Undoable (MeshEditCommand.capture),
  status "Recalculated normals". Operates on selected faces, or all when none.
- **Shift+E crease** — modal `src/tools/creaseOp.ts` (pure `creaseWeightFromDrag`
  extracted + tested): horizontal drag / typed 0..1 / −1 clears, live setCrease
  each frame, LMB-Enter confirm, RMB-Esc restore, undoable. Creased-edge cage
  highlight SKIPPED (editOverlayPass is out of scope, as the spec allows) — the
  creased subsurf is only visible on return to Object Mode since modifiers are
  hidden in the edit cage (existing behavior).
- **Ctrl+Alt+Numpad0 camera-to-view** — pure `src/tools/cameraToView.ts`
  (`cameraTransformFromView` + `quatFromBasis`), unit-tested round-tripping
  through P8-2's `cameraViewMatrix`. Snaps the active camera (TransformCommand)
  or creates one via `scene.addCamera` + `AddObjectsCommand` when none exists.
  Status "Camera set to view".
- **X-ray (Alt+Z + topbar chip)** — shared `xrayState` exported from
  elementPickPass; Alt+Z toggles it, the 👓 X-ray topbar chip mirrors/toggles
  it. Element-pick pass renders WITHOUT the occluder prepass and with depth-test
  disabled when ON, so hidden (back-side) elements become click-selectable.
- helpOverlay rows added for all five shortcuts.

DEVIATION (honest adjustment per the spec's "verify feasibility, adjust
honestly"): **box-select X-ray was NOT implemented.** Box select is a pure
screen-projection test with no occlusion; making it occlusion-aware for the
non-x-ray case (so OFF selects fewer) requires culling the cube's hidden back
corner — but the frozen `e2e/edit.mjs` asserts a whole-cube box selects all 8
verts with x-ray off, and that suite must pass unmodified. So box select selects
all-in-rect in both modes (unchanged), and X-ray select-through is delivered on
the element-pick (click) path instead. The p9-select e2e demonstrates it by
clicking the cube's occluded back corner: not selected with X-ray OFF, selected
with X-ray ON.

Minor scope note: added `src/tools/cameraToView.ts` (+test) — the spec asked for
a "pure helper + unit test" for camera-to-view but didn't name a file; a small
dedicated module was the cleanest home. Batch A is already merged and I'm the
sole worker, so no file-discipline conflict.
