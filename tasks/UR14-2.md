# UR14-2 — Topbar & outliner batch (UI-REVIEW.md items 5, 6, 7, 10, 11)

Read UI-REVIEW.md:
1. **Topbar clusters** (5): [Workspace tabs] · [Mode chip + Snap/X-ray/
   Overlays/Pivot] · [File ▾ menu: Save/Open/Import OBJ/Export OBJ] ·
   [Render] · right: [theme/help]. Visual separators (spacing + hairline).
   File menu = the add-menu popover pattern. Keyboard shortcuts unchanged.
2. **Label the two round toggles** (6) or fold into Overlays — read what they
   do first (grep topbar.ts) and pick the honest home; report the choice.
3. **No wrapped buttons** (7).
4. **Outliner calm-down** (10): selected row = left accent bar + tinted row
   (NOT solid orange slab); per-row kind glyphs always visible; eye/× on hover
   for every row; faint indent guides for parent chains.
5. **"+ New Collection" → small + button** in the outliner header (11).
STRICT scope: topbar.ts, outliner.ts(+css), theme.css additions scoped to
these. Do NOT touch materialTab/propertiesEditor/render engines.
Acceptance: tsc, units, extend/adjust affected e2e (outliner + any suite
clicking topbar buttons — grep first!), new checks: File menu opens/contains
4 items and each fires (download probes as existing save/export e2e do);
selected outliner row NOT full-saturation (computed style probe); hover
reveals ×. Screenshots before/after topbar + outliner, LOOKED at. Free-text
report, FIRST LINE STATUS:.
