# P6-6 — Shortcut overlay + status-hint audit

Read `tasks/P4-CONVENTIONS.md` first. Runs LAST (documents everything).

## Goal
The F1 overlay's SHORTCUTS array is stale — Phases 4-6 added many keys. Audit
`src/input/InputManager.ts` (EVERY key branch) plus workspace (Ctrl+Space),
N-panel (N), proportional (O), Delete alias, Ctrl+E/B/D, F, M, Period, and the
topbar-only actions; regroup the overlay (General / Object Mode / Edit Mode /
Camera / File / Workspaces). Also refresh the splash quick-start if outdated.

## Files
- `src/ui/helpOverlay.ts` (SHORTCUTS + grouping), `src/ui/splash.ts` (only if
  outdated), `e2e/ship.mjs` (update spot-checks to cover 3 NEW entries, e.g.
  Ctrl+B, N, O).

## Acceptance criteria
1. A grep-audit table in your Result section mapping every InputManager key
   branch → overlay entry (no omissions).
2. e2e spot-checks pass; all four suites + build + tests pass.
