# P15-2 — Interpolation coverage + N-panel/properties keying affordances

Read `tasks/P9-CONVENTIONS.md` first. Runs AFTER P15-1 (timeline exists),
PARALLEL with P15-3. Touch only your files. Ambiguous → STOP.

## Core already built (do NOT modify)
- fcurve.ts already EVALUATES constant/linear/bezier (Catmull-Rom auto
  Hermite). insertKey takes an interp param (default 'bezier').
- InsertKeysCommand.perform(..., interp). Timeline pane (P15-1) shows
  diamonds; P15-3 adds moving/deleting keys.

## Files (yours alone)
- NEW `src/core/anim/interp.test.ts` — the deep evaluation test suite
- NEW `src/ui/keyingMenu.ts` (+ css if needed)
- `src/input/InputManager.ts` — ONLY: change the I key to open your keying
  menu (kept tiny) instead of always-LocRotScale
- NEW `e2e/p15-keying.mjs`

## interp.test.ts (harden the evaluator)
Property-style coverage of evalFCurve: single-key curves return that value
everywhere; bezier through 3+ irregularly spaced keys passes through every
key exactly; constant spans hold the LEFT value right up to (but not at) the
next key; mixed-interp curves (constant→linear→bezier spans) evaluate each
span by ITS left key's mode; fractional frames; equal-value keys stay flat
(no overshoot: bezier between equal values must be exactly that value —
Catmull-Rom guarantees it, prove it); large frame numbers.

## Keying menu (Blender's I menu)
`keyingMenu.ts`: small popup (AddMenu widget pattern) listing Location /
Rotation / Scale / LocRotScale (default highlighted). Each entry:
InsertKeysCommand with the matching channel subset at scene.frameCurrent for
the selection. I key opens it at the pointer (object mode; empty selection →
status message, no menu). Esc/outside closes. A second I confirms the
highlighted default (LocRotScale) — i.e. I,I keys LocRotScale like before.

## Acceptance criteria
1. interp.test.ts all green (≥ 10 focused cases).
2. e2e (p15-keying.mjs): I opens the menu; clicking Location keys ONLY
   location channels (3 fcurves); I,I keys all 9; Ctrl+Z undoes one menu
   action wholesale; Esc closes without keying.
3. All e2e suites pass under flock (donut excluded) + npm test + tsc clean.
4. Append ## Result + flip P15-2 in PLAN.md.

## Result
Built.

**interp.test.ts** (`src/core/anim/interp.test.ts`, 16 tests, all green): single-key
curves read that value everywhere (left/at/right) + empty→0; bezier passes through
every irregularly-spaced key exactly and clamps flat outside; constant holds the
LEFT value up to (but not at) the next key; linear straight-line; mixed-interp curve
(constant→linear→bezier spans) evaluates each span by ITS left key's mode; equal-value
keys stay perfectly flat with NO overshoot (2-key span, 3+-key plateau sampled every
0.25f to 12-dp, and flat→peak→flat still hits each key); fractional frames; large frame
numbers (1e6 span + past-last-key clamp, and bezier hitting keys exactly at large frames).

**Keying menu** (`src/ui/keyingMenu.ts` + `src/ui/keyingMenu.css`): `KeyingMenu` — a
self-contained AddMenu-pattern popup (owns element + window keydown/pointerdown
listeners, idempotent close) listing Location / Rotation / Scale / LocRotScale.
LocRotScale is the highlighted default (`.key-menu-item.default`). Each entry runs
`InsertKeysCommand.perform` with its channel subset at `scene.frameCurrent` for the
snapshot selection, pushed to the undo stack (one wholesale entry). Esc / outside-click
close without keying. The menu's own capture-phase keydown intercepts a second I and
calls `confirmDefault()` → keys LocRotScale (I,I), matching the old plain-I.

**InputManager** (`src/input/InputManager.ts`, ONLY the I branch): object-mode I now
opens `KeyingMenu` at the pointer instead of always-LocRotScale; empty selection →
status message, no menu; a second I while open is handled by the menu (confirm), and
`this.keyingMenu` is nulled on close. Removed the now-unused `InsertKeysCommand`/
`LOC_ROT_SCALE` import from this file (they live in the menu now).

**e2e** (`e2e/p15-keying.mjs`, 19 checks, all pass under flock): I opens the menu;
Esc closes without keying; clicking Location keys exactly the 3 location.* fcurves;
Ctrl+Z removes them wholesale; I,I keys all nine LocRotScale channels; Ctrl+Z undoes
that wholesale; empty selection opens no menu.

Verify: `npx tsc --noEmit` clean; `npx vitest run` 616/616 (interp 16/16); `e2e/p15-keying.mjs`
19/19; smoke + edit + workspace suites pass under flock. No spec contradictions encountered.
