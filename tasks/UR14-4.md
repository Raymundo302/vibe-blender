# UR14-4 — Navigation batch (UI-REVIEW.md item 14)

1. **Orientation gizmo**: mini axis widget top-right of the viewport (inside
   #viewport-wrap, canvas-drawn or DOM): three colored axis dots/lines
   tracking the camera per frame; CLICK an axis = snap the view to it
   (front/right/top + negatives), animated over ~150ms.
2. **Numpad view shortcuts**: 1 front (−Y), 3 right (+X), 7 top (+Z), 9
   opposite, Ctrl+1/3/7 = negatives, 5 = ortho toggle IF cheap (we're
   perspective-only — if ortho is non-trivial, SKIP and document; do not
   build a projection mode in this batch). Numpad0 camera-view stays.
3. F1 overlay + hint bar (if UR14-1 landed) list the new keys.
STRICT scope: new axisGizmo module, InputManager numpad branch, helpOverlay.
Acceptance: tsc, units, e2e: click the gizmo's X axis → camera yaw/pitch snap
to the right view (probe camera state), numpad 1/3/7 likewise incl. Ctrl
variants; gizmo tracks orbit (probe positions at two yaws); no interference
with existing numpad0. Screenshot of the gizmo, LOOKED at. Free-text report,
FIRST LINE STATUS:.
