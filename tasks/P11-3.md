# P11-3 — Textures: checker + image maps through UVs

Read `tasks/P9-CONVENTIONS.md` (same rules) and `tasks/P11-0-DESIGN.md`.
Parallel with P11-1 (unwrap/menus/InputManager) and P11-2 (uvEditor/main.ts) —
touch only your files.

## Core provided (F11-1, committed — do NOT modify beyond listed files)
Material gains `texKind: 'none' | 'checker' | 'image'` and `texDataUrl:
string | null` (+ runtime `texImage` decoded-pixels cache, worldData-style);
serialized in sceneJson. meshToGpu emits per-corner UVs at attribute
location 3 (0,0 when a face has no UVs). Snapshot triangles carry per-corner
UVs (`triUV`). Your job is CONSUMING all of it.

## Files (yours alone)
- `src/render/passes/renderedPass.ts` — sample the texture: checker =
  procedural in-shader (u_texKind uniform; ~8×8 via floor(uv*8)), image =
  bound TEXTURE_2D (Renderer uploads — see next line); multiply into baseColor
  before lighting.
- `src/render/Renderer.ts` — lazy per-material texture upload cache
  (dataUrl → WebGLTexture, keyed by material id + a version/url check),
  mirroring the P10-4 HDRI upload pattern; bind per draw in rendered mode.
- `src/renderEngine/tracer.ts` (+ snapshot.ts if needed) — barycentric UV
  interpolation at hit points; checker procedural / image bilinear sample
  multiplied into baseColor. Deterministic, no RNG changes.
- `src/ui/materialTab.ts` — texture rows: kind select (None/Checker/Image),
  image file input + thumbnail when kind=image (an <img>, NOT a canvas),
  undoable via the existing command patterns (image load = one command with
  old/new url, worldTab HDRI style).
- NEW unit test file(s); NEW `e2e/p11-textures.mjs`

## Acceptance criteria
1. Unit: tracer checker — two rays hitting known UV regions of a unit quad
   (seeded, tiny render or direct hit-shade probe) return alternating colors;
   image sample — a 2×2 test image maps corners correctly (bilinear at
   centers); material serialization round-trips texKind/texDataUrl
   byte-identically.
2. e2e: cube with programmatic UVs (seed directly; don't depend on P11-1),
   material → Checker via the Material tab, rendered mode → viewport shows
   alternating light/dark squares on a face (probe 2 pixels expected to
   differ); F12 render sky-vs-cube unchanged but cube pixels show checker
   variance; switch to a tiny generated data-URL image → dominant color
   changes accordingly; undo returns to None; save→load round-trip keeps the
   texture (byte-equal serialize).
3. Materials with texKind 'none' render EXACTLY as before (regression: a
   pre-P11 scene's rendered pixels unchanged — spot-probe).
4. All e2e suites (flock, donut excluded) + npm test + tsc clean.

## Result
DONE (2026-07-07).

Implemented base-color textures (checker + image) consumed everywhere UVs flow:

- `src/render/passes/renderedPass.ts` — vertex shader now reads per-corner UV at
  location 3 (`v_uv`); fragment multiplies a texture term into `baseColor` before
  F0/lighting: `u_texKind` 1 = procedural 8×8 checker (`mod(floor(uv*8)…)`, even
  cell → 0.2 dark, odd → 1.0 light), 2 = bound `sampler2D u_tex` (guarded by
  `u_hasTex`). `RenderedPass` gained a 1×1 white fallback texture + `bindTexture()`
  so the sampler unit is always valid; `setObject` sets `u_texKind`.
- `src/render/Renderer.ts` — lazy per-material texture cache (`matTextures`, keyed
  by material id + data-URL, async decode like the P10-4 HDRI upload,
  `SRGB8_ALPHA8`); binds per draw in the rendered loop (white when none/checker).
- `src/renderEngine/snapshot.ts` — `SnapMaterial` carries `texKind` + decoded
  `texImage` (worldData-style, image materials only); `toMat` populates them.
  `triUV` was already emitted by F11-1.
- `src/renderEngine/tracer.ts` — `TraceScene.triUV`; per hit, barycentric UV
  interpolation (A=1-u-v, B=u, C=v) feeds `sampleMaterialTexture` (checker parity
  matches the GLSL; image = `sampleImageBilinear`, clamp-to-edge). The multiplier
  scales an effective albedo used in direct lighting + every bounce. 'none' → ×1,
  so pre-P11 scenes are byte-identical (no new RNG draws).
- `src/ui/materialTab.ts` — Texture rows: kind select (None/Checker/Image) + image
  file input & `<img>` thumbnail (image only). `TextureEditCommand` makes kind
  changes and image loads each one undo step (restores decoded cache too).
  `decodeTextureDataUrl` + `__materialTab` debug handle (mirrors `__world`).
- Tests: `src/renderEngine/textures.test.ts` (7 tests — checker alternation via
  direct probe AND a two-ray unit-quad trace giving exact albedo·energy/π contrast;
  2×2 image bilinear at texel centers; none/no-pixels → white; serialize
  round-trip). `e2e/p11-textures.mjs` (checker via the tab → viewport strip
  variance 73 vs 17 baseline; F12 render checker variance + unchanged sky; tiny
  red data-URL image → cube reads red; undo image→checker→none; save/load
  byte-equal).

Verification: `npx tsc --noEmit` clean; `npm test` 55 files / 452 tests green;
`e2e/p11-textures.mjs` green x3; existing suites green (smoke, edit, workspace,
ship, p8-*, p9-* incl. p9-render2, p10-camera2/collections/themes).

KNOWN CROSS-WORKER ISSUE (not P11-3): `e2e/p10-world.mjs` fails ONE check —
"donut fixture has no world key (pre-P10-4 file)". A parallel worker rewrote
`research/donut.vibe.json` today to include a `world` key, so that assertion no
longer holds. Neither `research/donut.vibe.json` nor `e2e/p10-world.mjs` is a
P11-3 file, and none of my changes touch world serialization; my code does not
cause this. Flagged for the orchestrator to reconcile (the fixture or the p10
assertion needs updating by whoever owns the donut re-save).
