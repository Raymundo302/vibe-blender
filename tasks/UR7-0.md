# UR7-0 — INVESTIGATE (do not fix): wheel-zoom "stuck in-between frame"

Ray's report: "the mouse wheel zoom in regular mode sometimes gets stuck in this
sort of in-between frame." Screenshot: `research/zoom-stuck-frame.png`
(taken 2026-07-10 ~14:07, so pre-UR6-ribbons build). Visible symptoms in it:
- The selected plane's orange OUTLINE draws straight across the cube as a
  dashed line (outline visible through occluding geometry);
- the cube's front face is blown out to near-white while its top face shades
  normally (matcap looks wrong on one face only);
- per Ray this state PERSISTS on screen until some next input ("stuck").

## Your job — root cause only
This is an investigation task in the AO-saga tradition (CLAUDE.md "AO/debugging
playbook"). Deliver a root-cause analysis, not a patch.
1. Read the frame-loop / render scheduling in src/main.ts + Renderer.render —
   is rendering continuous rAF or damage-driven? What could leave a partial
   frame on screen?
2. Reproduce: drive wheel-zoom bursts (many wheel events per frame, plus zoom
   during other work — SDF atlas rebuild throttle, intersection rebuild
   throttle, texture uploads, outline mask resize) in headless on BOTH
   SwiftShader and E2E_GPU=1, screenshotting every frame (orbit-sweep style)
   and diffing consecutive frames for the two signatures (outline-through-
   geometry px; face luminance far above matcap range). An automated detector
   beats eyeballs here.
3. Prime suspects to check (verify, don't assume): outlinePass mask FBO resize
   vs canvas resize ordering during zoom (DPR/zoom → mask a frame behind =
   outline drawn from a stale mask → dashed through-geometry look); depth
   buffer cleared/mid-pass state leak between outline mask and matcap pass
   (blown white face = matcap sampling a bound-but-wrong texture unit, e.g.
   the AO or mask texture left bound); wheel handler re-entrancy (zoom inside
   a render tick).
4. The screenshot pre-dates UR6's ribbon/outline rework — check whether the
   16-tap outline landing changed the failure surface, and reproduce on the
   CURRENT tree.

## Output
A written analysis: reproduction recipe (script path in scratchpad), root
cause with evidence (which pass, which state, why it persists), whether UR6
already changed it, and the minimal fix you would make (do NOT apply it).
If you cannot reproduce after a serious attempt (≥ the full suspect list),
report exactly what you tried and what instrumentation to add for live capture
on Ray's session.

Work read-only except scratchpad scripts + research/zoom-* evidence images.
E2E_PORT=9475. Dev server at :5199. If blocked, report blocked.
