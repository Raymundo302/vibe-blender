# P14-4 — Node-shading dry run (runs AFTER P14-1/2/3 verify)

Read `tasks/P9-CONVENTIONS.md` first. You run ALONE (no parallel workers).
Ambiguous → STOP.

## Context
P14-1 built the Shader Editor pane, P14-2 the tracer hook + checker/noise/
mixColor, P14-3 imageTexture/colorRamp/math. Read their specs' Result
sections first. The donut scene lives at
`e2e/fixtures/donut-p9-frozen.vibe.json` (loadable via `?scene=` or
__app.io.apply — see e2e/p11-uv-dryrun.mjs for the pattern; the icing object
and its material are identified there).

## Files (yours alone)
- NEW `e2e/p14-dryrun.mjs`
- NEW `research/NODES-RUN.md` (findings)
- NEW `research/donut-nodes.vibe.json` (the saved result scene)

## The run (public entry points wherever possible; __app where a pane is
impractical to drive)
1. Load the frozen donut; give the ICING material a node graph: noise →
   colorRamp (pink→white) → baseColor, noise → roughness (via math multiply
   0.5). useNodes on, version bumped.
2. Rendered viewport: screenshot differs from pre-nodes; no GL errors.
3. Shader Editor pane: open it on the icing object, assert it renders nodes
   (count the drawn node boxes via the debug handle P14-1 exposed).
4. F12 low-spp path trace: completes; differs from pre-nodes trace of the
   same view; save the PNG bytes to research/ as the hero shot if the harness
   supports it (p9-8's donut.mjs shows how renders were captured).
5. Save the scene (io.serialize) to research/donut-nodes.vibe.json; re-apply
   it into a fresh boot (t.reload + io.apply) and assert the graph survived
   (node count + link count + a re-render still differs from flat).
6. Write research/NODES-RUN.md: what worked, gaps found (missing nodes,
   UX pain, bake artifacts), each labeled FIXED-HERE / PUNCH-LIST.

## Acceptance criteria
1. p14-dryrun.mjs passes under flock; all other suites (donut.mjs INCLUDED
   this time) pass under flock; npm test + tsc clean.
2. NODES-RUN.md + donut-nodes.vibe.json exist and are honest.
3. Append ## Result + flip P14-4 in PLAN.md.

## Result
Built. The node-shading pipeline was driven end-to-end on the frozen donut; no
app code needed changing — this task is the proof, plus the three deliverables.

Files (mine alone):
- NEW `e2e/p14-dryrun.mjs` — loads `e2e/fixtures/donut-p9-frozen.vibe.json`,
  Smart-UV-Projects the icing (procedural noise reads surface UVs; the frozen
  icing has none), builds `noise → colorRamp(pink→white) → baseColor` +
  `noise → math(×0.5) → roughness` on the icing material (id 0, `Material.001`),
  then asserts: Rendered-viewport bake differs from the flat baseline (1,794 px,
  0 GL errors, baked base texture whose version tracks `nodeGraphVersion`); the
  Shader Editor pane (`wsp-area-select` → `shader`) draws 4 node boxes
  (`window.__shaderEditor.nodeCount()`) with laid-out sockets; a low-spp (4) F12
  trace completes and differs from the flat trace of the same view (meanAbsDiff
  ≈ 6.4/255); io.serialize → reload (`t.reload`) → io.apply keeps the graph
  (4 nodes + 4 links, `useNodes` on) and still shades differently than flat.
- NEW `research/donut-nodes-hero.png` — the F12 hero shot (960×540).
- NEW `research/donut-nodes.vibe.json` — the saved node-material scene.
- NEW `research/NODES-RUN.md` — findings (all PUNCH-LIST, nothing needed
  FIXED-HERE): top gap is the lack of generated/object texture coordinates
  (procedural nodes need a UV map, so the icing had to be unwrapped first);
  plus noise-centres-near-0.5 washes out a 2-stop ramp, no scriptable
  add-node/add-link public API (graph built via `material.nodeGraph`), and a
  bake-resolution-vs-UV-density note.

Tiebreaker note: no spec contradiction. The spec asked for the noise→ramp graph
but the frozen icing has no UVs; rather than change the graph I added the minimal
enabling step (Smart UV Project, the proven public menu flow from
`e2e/p11-uv-dryrun.mjs`) and logged the missing texture-coord node as the top
PUNCH-LIST finding — exactly the kind of gap this dry run exists to surface.

Verification: `npx tsc --noEmit` clean; `npm test` 600/600; under flock —
`e2e/p14-dryrun.mjs`, `e2e/donut.mjs` (included this time), smoke, edit,
workspace all pass.
