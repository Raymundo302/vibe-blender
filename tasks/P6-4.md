# P6-4 — Autosave + restore

Read `tasks/P4-CONVENTIONS.md` first.

## Goal
Every 30s (and on visibilitychange→hidden), serialize the scene to
localStorage `vibe-blender-autosave` (with a timestamp field wrapper). On boot,
if an autosave exists and differs from the pristine default scene, show a
small toast: "Restore previous session? [Restore] [Discard]" — Restore applies
it (undo cleared), Discard deletes it. Saving/loading a file clears the
autosave (the file is now the source of truth).

## Files
- NEW `src/io/autosave.ts` (start/stop, save-now, load/clear; interval owned
  here); NEW `src/ui/restoreToast.ts`; `src/main.ts` wiring; `src/ui/theme.css`;
  `e2e/ship.mjs` appended. Expose `__app.autosave = { saveNow, clear }` for e2e.

## Acceptance criteria
1. e2e: mutate scene → `__app.autosave.saveNow()` → reload → toast appears →
   Restore brings the mutation back; second flow: Discard → default cube scene
   and autosave key gone. Timer NOT tested (interval injected/config, unit-test
   the wrapper shape instead).
2. Boot with no autosave shows no toast (existing suites keep passing — they
   reload often; ensure the toast only appears when an autosave EXISTS and
   differs from the default scene, and that dismissing splash doesn't race it).
3. All four e2e suites + npm run build + npm test pass.

## Result
Built. `src/io/autosave.ts` — pure `wrapAutosave`/`parseAutosave` envelope
(`{format,version,savedAt,scene}`) plus an `Autosave` class owning the injectable
interval (default 30s) + save-on-`visibilitychange→hidden` + best-effort
localStorage under `vibe-blender-autosave`. `src/ui/restoreToast.ts` — a small
`pointer-events:none` card (only its buttons are interactive) shown on boot when
a stored autosave differs from the pristine default-scene snapshot; Restore
applies it via `applySceneJson` + `undo.clear()`, Discard deletes the key. In
`main.ts`, `saveScene()` and `loadSceneJson()` (file save/open + `io.apply`) clear
the autosave so a file becomes the source of truth. Exposed
`__app.autosave = { saveNow, clear }` for e2e.

Wrapper shape unit-tested in `src/io/autosave.test.ts` (interval NOT unit-tested,
per spec — driven by e2e instead). `e2e/ship.mjs` appended: FLOW 1 mutate →
saveNow → reload → toast → Restore restores the mutation; FLOW 2 Discard → default
cube + key gone; plus no-autosave→no-toast and file-load-clears-autosave checks.

Verified: `npm run build` clean, `npm test` 198 pass (24 files), all four e2e
suites pass sequentially (ship 128/128, incl. 13 new P6-4 checks). Note: after
`Page.reload` a settle `sleep(200)` follows `until('!!window.__app')` — the CDP
execution context briefly swaps during navigation, matching the existing P3-4
reload pattern.
