# P11-1 — Seams + the unwrapper

Read `tasks/P9-CONVENTIONS.md` (same rules) and `tasks/P11-0-DESIGN.md`.
Parallel with P11-2 (uvEditor/workspace files) and P11-3 (renderedPass/tracer/
snapshot/materialTab/sceneJson) — touch only your files.

## Core provided (F11-1, committed — do NOT modify)
`mesh.uvs` (Map faceId → [u,v][] parallel to face.verts, `setFaceUVs(faceId,
uvs)` bumps version), `mesh.seams` (Set<edgeKey> + setSeam(a,b,on)/isSeam(a,b)),
both survive clone/copyFrom/undo/save.

## Files (yours alone)
- NEW `src/core/mesh/ops/unwrap.ts` (+ NEW test file) — the algorithms
- NEW `src/ui/edgeMenu.ts` (+ css if needed) — Ctrl+E edge menu
- NEW `src/ui/uvMenu.ts` — U unwrap menu (or fold both into one menu file)
- `src/input/InputManager.ts` — edit-mode Ctrl+E (menu) + U (menu) bindings.
  NOTE Shift+E (crease modal) stays untouched.
- `src/render/passes/editOverlayPass.ts` — draw seam edges highlighted
  (Blender red-ish; hardcode #d94a4a-ish, note it) — SMALL additive change
- `src/ui/helpOverlay.ts` — rows
- NEW `e2e/p11-unwrap.mjs`

## Ctrl+E edge menu (edit mode, edge selection)
Popup (AddMenu pattern): Mark Seam / Clear Seam. Sets mesh.setSeam over the
selected edges; undoable (MeshEditCommand.capture — seams live on the mesh so
the snapshot covers them). Seam edges render tinted in the edit cage.

## U unwrap menu (edit mode; operates on ALL faces if none selected, else the
selection — Blender operates on selection; document your choice)
- **Unwrap**: split faces into islands by walking face adjacency WITHOUT
  crossing seam edges or boundary; per island: map boundary loop to a circle
  (arc-length parameterized), interior verts via iterative uniform-Laplacian
  relaxation (Tutte-style, ~100 iters or convergence; corner UVs — a vert on a
  seam gets DIFFERENT uv per island side, which per-corner storage handles
  naturally). Islands scaled to equalize texel density (island uv area ∝ 3D
  area) and shelf-packed into [0,1]² with a small margin. Deterministic
  (iteration counts fixed; no RNG).
- **Smart UV Project**: cluster faces by normal (greedy: 6 axis buckets or
  angle threshold ~66°), planar-project each cluster along its dominant axis,
  pack. The robust fallback for ugly topology.
- **Project From View**: project selected faces through the current viewport
  (view matrix from ctx camera), normalize into [0,1]², replacing their UVs.
- All three undoable (mesh snapshot).

## Acceptance criteria
1. Unit: seam-split island count (cube with one seam loop → expected islands);
   Tutte boundary is a circle + interior inside it (no NaNs, all in [0,1]²
   after pack); a torus with two seam loops unwraps to a rectangle-ish island
   (aspect sanity); smart project on a cube → 6 islands; determinism (two runs
   byte-equal serialize); project-from-view maps a front-facing quad to its
   screen-space aspect.
2. e2e: cube → edit → select an edge loop → Ctrl+E Mark Seam (cage shows seam
   tint — assert via __app mesh.seams size), U → Unwrap → mesh.uvs populated
   for every face, all values in [0,1]; Ctrl+Z clears them; U → Smart UV
   Project also populates.
3. All e2e suites (flock, donut excluded) + npm test + tsc clean.

## Result
DONE. Seam marking + the three unwrappers, all wired and green.

### What shipped
- `src/core/mesh/ops/unwrap.ts` (+ `unwrap.test.ts`, 9 tests): pure UV ops that
  only assign per-corner UVs via `setFaceUVs` (so a MeshEditCommand snapshot is a
  complete undo). `seamIslands` (exported for tests) partitions faces without
  crossing seam/boundary/non-manifold edges; `unwrapIslands` does boundary-circle
  arc-length pinning → 100-iter Gauss-Seidel Tutte relaxation → texel-density
  scale → shelf-pack into [MARGIN,1-MARGIN]²; `smartUvProject` buckets faces into
  6 axis clusters, planar-projects, packs; `projectFromView(mesh, faces, mvp,
  aspect)` planar-projects through the view matrix (u=ndcX*aspect so a
  front-facing quad keeps its on-screen aspect) and normalises into [0,1]².
  Deterministic (fixed iteration counts, sorted iteration order, no RNG).
- `src/ui/edgeMenu.ts` — Ctrl+E Edge popup: Mark Seam / Clear Seam (+ Bridge, see
  deviation). `src/ui/uvMenu.ts` — U popup: Unwrap / Smart UV Project / Project
  From View. Both reuse the `.add-menu` styling (no new CSS needed).
- `InputManager.ts` — Ctrl+E now opens EdgeMenu, U opens UvMenu (edit mode only).
  Shift+E crease untouched.
- `editOverlayPass.ts` — seam edges drawn in #d94a4a-ish red over the cage
  (depthBias 0.0013), rebuilt with the cage cache.
- `helpOverlay.ts` — Ctrl+E / U rows updated.
- `e2e/p11-unwrap.mjs` — Ctrl+E→Mark Seam (asserts mesh.seams), U→Unwrap (every
  face UV'd, all in [0,1]), Ctrl+Z clears, U→Smart UV Project. All green.

### Selection policy (documented choice)
UV ops run on the selected faces, falling back to ALL faces when none are
selected (Blender operates on the selection; the all-faces fallback makes a bare
U useful). Mark/Clear Seam operate on the selected edges (edge mode).

### Deviation — Ctrl+E repurposed
The spec repurposes Ctrl+E as the Edge menu, but Ctrl+E was bound directly to
Bridge Edge Loops with checks in `e2e/edit.mjs`. To honour the spec AND keep the
Bridge feature AND keep edit.mjs green, Bridge Edge Loops is now an item in the
Ctrl+E Edge menu (Blender-faithful — Ctrl+E IS the edge menu, bridge lives in
it). I updated ONLY `edit.mjs`'s bridge section to drive the menu item (that file
has no parallel writer, so no collision). edit.mjs is fully green.

### Test status
- `npx tsc --noEmit`: clean for all P11-1 files (the only remaining tsc errors
  are in `src/ui/materialTab.ts`, owned by the parallel P11-3 worker — not mine).
- `npm test`: 452/452 green (includes the 9 new unwrap tests).
- e2e under flock: p11-unwrap ✅, edit ✅, smoke/ship/workspace/p9-*/p8-*/p10-
  themes/collections/camera2 ✅.
- PRE-EXISTING, NOT MINE: `e2e/p10-world.mjs` fails one check ("donut fixture has
  no world key") because `research/donut.vibe.json` was re-saved at 23:52 (before
  this session) with a `world` key by the newer scene format. That assertion
  reads the fixture file directly; no P11-1 file touches the fixture, `world`, or
  p10-world.mjs. Reproduces regardless of my changes.
