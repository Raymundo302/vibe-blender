# P16-2 — Texture Coordinate (Generated) + Map Range node + noise contrast

Read tasks/P9-CONVENTIONS.md first. PARALLEL with P16-1/3/4. Ambiguous →
STOP; existing code is the tiebreaker (P14-3 lesson: sampling conventions!).

## Context (may modify where stated)
- NODES-RUN.md finding #1: procedural nodes collapse without a UV map.
  Plan: per-corner GENERATED coordinates = object-space position normalized
  to the object's local-mesh AABB (Blender's Generated).
- EvalContext (src/core/nodes/nodeGraph.ts — you may EXTEND the interface
  only: add optional `gen?: [number, number, number]`).
- Tracer snapshot (src/renderEngine/snapshot.ts): mirrors triUV with a NEW
  triGen Float32Array (3 floats per corner: the corner vert's LOCAL position
  normalized to the base evaluated-mesh AABB, computed once per object).
  tracer.ts interpolates it like triUV and passes ctx.gen. Non-node
  materials: ZERO behavior change — tracer.test.ts must stay bit-identical
  (empty diff, 19/19).
- bake.ts (Rendered path) has no surface positions: ctx.gen stays undefined
  there → the node falls back to (u, v, 0). Document this honest limitation
  in the node file + your Result.

## Files (yours alone)
- NEW src/core/nodes/nodesC.ts (+ one import line in builtins.ts) + tests
- src/core/nodes/nodeGraph.ts — the optional EvalContext field ONLY
- src/renderEngine/snapshot.ts + src/renderEngine/tracer.ts — triGen only
- src/core/nodes/nodesA.ts — noise gains a `contrast` float param (0..1,
  default 0: raw fbm; higher = smoothstep-style mid push; deterministic,
  and contrast 0 must be BIT-IDENTICAL to today's output — nodesA.test.ts
  must keep passing unchanged)
- NEW e2e/p16-texcoord.mjs

## Nodes (nodesC.ts)
- texCoord: outputs generated(vector) = ctx.gen ?? [ctx.u, ctx.v, 0], and
  uv(vector) = [ctx.u, ctx.v, 0].
- mapRange: inputs value(float 0.5), params fromMin/fromMax/toMin/toMax
  (floats, defaults 0/1/0/1) + clamp(select yes/no default yes); output
  value. Degenerate fromMin==fromMax → toMin (document).

## Acceptance criteria
1. Unit: texCoord fallback + gen passthrough; mapRange remap/clamp/degenerate;
   noise contrast=0 bit-identity + contrast monotonicity (higher contrast →
   values pushed away from 0.5); triGen normalization on a known cube
   (corners map to 0/1 per axis).
2. e2e (p16-texcoord.mjs): load the frozen donut fixture; give the icing
   (NO UV unwrap this time!) texCoord.generated → noise.uv → colorRamp →
   baseColor with useNodes; F12 low-spp: the traced icing VARIES (differs
   from a flat-material trace) — the exact gap NODES-RUN found, now closed.
3. tracer.test.ts empty diff + 19/19; all e2e under flock (donut excluded) +
   npm test + tsc clean.
4. ## Result + PLAN.md flip.

## Result
(pending)
