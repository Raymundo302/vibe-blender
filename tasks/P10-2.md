# P10-2 — Camera: Lock to View + passepartout

Read `tasks/P9-CONVENTIONS.md` first (P10 runs by the same rules). Runs AFTER
the P10-1/P10-3 batch — sole worker in the tree.

## Goal
Blender's two camera-view comforts: **Lock Camera to View** (navigate the
viewport while in camera view and the camera follows) and the **passepartout**
(darkened area outside the render frame when looking through a camera).

## Files (yours alone)
- `src/core/scene/objectData.ts` — CameraData gains `lockToView: boolean`
  (default false; update defaultCamera)
- `src/io/sceneJson.ts` — serialize it (optional on load → false; mirror how
  camera fields are handled)
- `src/ui/cameraTab.ts` — "Lock to View" checkbox (undoable via the existing
  CameraCommand pattern)
- `src/input/InputManager.ts` — camera-view navigation branch
- NEW `src/ui/passepartout.ts` (+ NEW `src/ui/passepartout.css`)
- `src/render/Renderer.ts` — ONLY if you need a tiny hook to know the
  camera-view frame rect; prefer computing it in passepartout.ts from the
  same inputs (canvas size + cameraFovY)
- `src/ui/helpOverlay.ts` — note under Numpad0 if wording changes
- NEW unit test file for the pure rig math
- NEW `e2e/p10-camera2.mjs`

## Lock to View
Today, orbit/pan/zoom while in camera view EXITS the view (P8-2). New rule:
if the viewed camera's `lockToView` is true, navigation stays in camera view
and MOVES THE CAMERA instead:
- On entering camera view (Numpad0) with lock on — or when the checkbox turns
  on while in camera view — initialize a private orbit rig from the camera
  pose: eye = camera position, forward from objectForward, target = eye +
  forward × d where d = the camera's distance to the world origin projected
  onto forward, clamped to 1..50 (document; Blender uses the view's own dolly
  target, this is our stand-in).
- Orbit/pan/zoom mutate the rig (reuse OrbitCamera math — you may instantiate
  an OrbitCamera as the rig; do NOT touch the main viewport OrbitCamera).
- Each nav event writes the rig pose back to the camera object's transform
  (position = rig eye, rotation = look-at quaternion; reuse/extract the
  cameraToView helper math from P9-6).
- Undo: ONE TransformCommand per camera-view session, pushed when leaving
  camera view (or toggling lock off) if the pose changed — continuous nav must
  not spam the undo stack (document this Blender-ish behavior).
- lockToView false → today's exit-on-orbit behavior, unchanged.

## Passepartout
When (and only when) the renderer is in camera view: a DOM overlay inside
#viewport-wrap that dims everything outside the render frame — the largest
16:9 rect that fits centered in the viewport (16:9 = the tracer's output
aspect). Four absolutely-positioned divs (top/bottom/left/right) with
`background: rgba(0,0,0,0.55)` + a 1px border rect on the frame (use theme
var --vb-border or a dedicated --vb-passepartout if you add it to BOTH themes
in themes.ts — smaller diff wins). Recompute on resize + camera-view
enter/exit (a per-frame update() call from main.ts's frame loop is fine and
matches the panels' pattern — you may add ONE wiring line to main.ts for it).
Pointer-events: none — it must never eat viewport input.

## Acceptance criteria
1. Unit: rig round-trip (camera pose → rig → pose is identity within epsilon);
   orbiting the rig keeps the target fixed; pose-changed detection.
2. e2e: camera with lockToView OFF: MMB drag exits camera view (existing
   behavior asserted). Toggle lock ON via the Camera tab: Numpad0, MMB drag →
   STILL in camera view (cameraViewId set) and the camera object's transform
   changed; leaving camera view pushes exactly ONE undo entry; Ctrl+Z restores
   the original pose. Passepartout: divs present + cover the expected pixels
   while in camera view (check a corner pixel is overlaid and a center pixel
   is not), absent outside camera view, and clicks still select through the
   frame area.
3. All e2e suites pass under flock (donut.mjs excluded — architect's final
   sweep) + npm test + npx tsc --noEmit.

## Result
DONE — Lock Camera to View + passepartout shipped, all gates green.

### What changed
- `src/core/scene/objectData.ts` — `CameraData.lockToView?: boolean` (optional so
  pre-lock scenes/tests parse → false); `defaultCamera()` sets it false.
- `src/io/sceneJson.ts` — serialize `lockToView` (always written as a bool);
  parse it optional → `c.lockToView === true` (v1/v2/pre-lock files → false).
- `src/ui/cameraTab.ts` — "Lock to View" checkbox (`.camera-tab-lock`
  [data-field="lockToView"]) committing ONE `CameraCommand` per toggle
  (full-CameraData snapshot, so undo restores it); `writeFields` syncs it. No
  css added (row reuses the existing `.camera-tab-row` layout).
- `src/input/InputManager.ts` — pure rig math EXPORTED for the unit test:
  `configureRigFromCamera(rig, transform)`, `cameraPoseFromRig(rig)`,
  `poseChanged(a, b, eps)`. Rig session: `ensureCamRig()` lazily seeds a private
  `OrbitCamera` from the camera pose on the FIRST nav event (covers both "entered
  camera view with lock on" AND "checkbox turned on while in view" with no
  cross-wiring); MMB orbit/pan + wheel zoom mutate the rig and `writeCamRig()`
  writes eye→position / look-at→rotation back to the camera each event;
  `finalizeCamRig()` pushes ONE `TransformCommand('Camera Nav')` on leave (or on
  the next unlocked nav) iff the pose changed — continuous fly never spams undo.
  Lock OFF keeps today's exit-on-orbit behavior.
- NEW `src/ui/passepartout.ts` (+ `passepartout.css`) — DOM overlay in
  `#viewport-wrap`, four `rgba(0,0,0,0.55)` mask panes + a 1px `--vb-border`
  frame rect around the largest centered 16:9 rect (tracer aspect). Shown only
  while `renderer.cameraViewId !== null`; `pointer-events:none` so clicks select
  through it. Pure `frameRect(w,h)` letterbox math exported. Frame computed in
  passepartout.ts from canvas size — Renderer.ts NOT touched.
- `src/main.ts` — construct `Passepartout` beside `nPanel`; ONE frame-loop line
  `passepartout.update()` (matches the panels' pattern).
- `src/ui/helpOverlay.ts` — Numpad0 note mentions Camera tab › Lock to View.

### Documented Blender-ish behavior / deviations
- Rig turntable target = world-origin distance projected onto forward, clamped
  1..50 (our stand-in for Blender's view dolly target). Rig up is +Y (roll-free).
- Continuous nav = ONE undo step, committed on leave (or next unlocked nav). If
  lock is unchecked mid-view without navigating again, the pending fly commits on
  the next leave rather than exactly at uncheck (no cameraTab→InputManager wiring
  exists); the pose is never lost and stays a single undo entry.

### Gates
- `npx tsc --noEmit` clean.
- `npm test` — 420 unit tests pass (new `src/input/cameraRig.test.ts`: rig
  round-trip within epsilon, 1..50 clamp, orbit keeps target fixed, poseChanged
  incl. quat double-cover).
- `e2e/p10-camera2.mjs` — 23/23 (lock OFF exits + no move; lock ON stays in view +
  moves; mid-fly undo empty; leave pushes EXACTLY ONE; Ctrl+Z restores pose;
  passepartout 4 masks + frame, corner overlaid / center clear / center in frame,
  click selects through, hidden on exit).
- All existing suites green under flock (donut.mjs excluded — architect's sweep):
  smoke, edit, workspace, p8-*, p9-* (incl. p9-render2 long), p10-collections,
  p10-themes, p10-world, ship.
