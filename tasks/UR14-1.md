# UR14-1 — Status & hints batch (UI-REVIEW.md items 1, 4, 15, 16, 17, 18)

Read UI-REVIEW.md — this batch = the "visibility of status" items:
1. **Modal-key hint bar** (item 1): one-line strip along the viewport bottom
   (above the timeline area, inside #viewport-wrap) that rewrites per context:
   idle object mode / idle edit mode (per element mode) / during EACH modal op
   (Move: "X/Y/Z axis · Shift precise · G slide · type number · LMB confirm ·
   Esc cancel"; slide/normal-move/rotate/scale/circle/lasso/knife/page mode/
   curve edit accordingly). Content from a per-operator hint registry —
   operators expose `hints(): string` or a static map in the bar module; keep
   strings SHORT. Muted styling, never intercepts pointers.
2. **Empty states point forward** (item 4): properties "No active object —
   Shift+A adds one"; empty timeline hint "Select an object and press I to
   key a pose".
3. **One mode chip** (item 15): a single top-left viewport chip that announces
   the current special mode + exit key: camera view ("Viewing Camera — Numpad0
   exits"), Page Mode, Curve Edit, Text Edit, Extract Element. Replaces the
   scattered per-feature chips (keep their logic, route the TEXT through one
   component).
4. **Full object name in the topbar info chip** (item 16) — stop truncating.
5. **Destructive toasts** (item 17): outliner ×, Convert to Mesh, modifier
   Apply → "Deleted Cube — Ctrl+Z restores" style toasts (reuse the status
   chip/toast infra; 2× current duration per item 19).
6. **Dirty dot on Save** (item 18): undo-stack position vs last save/load.
STRICT scope: new hintBar/modeChip modules + minimal touchpoints (InputManager
hint wiring, outliner/topbar/properties/timeline one-liners). Do NOT touch
materialTab/objectData/sceneJson/render engines (another chain owns them).
Acceptance: tsc, units, new e2e/ui-hints.mjs (hint bar text changes across
modes/ops — drive G and read the bar; empty states; dirty dot toggles on
save+edit; delete toast text), all regressions green. Screenshots of the bar
during a Move and the mode chip in Page Mode, LOOKED at. Free-text report,
FIRST LINE STATUS:.
