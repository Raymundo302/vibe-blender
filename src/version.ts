/**
 * Single source of truth for the app version string shown in the bottom status
 * bar (right side). Bump this when cutting a notable build; keep package.json's
 * "version" field in sync so both report the same number.
 */
export const APP_VERSION = '0.2.0';
