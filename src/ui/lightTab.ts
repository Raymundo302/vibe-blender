/**
 * Light properties tab (P8-1). Registered via registerPropertiesTab like
 * modifierTab. STUB — the P8-1 worker owns this file; nothing else registers
 * or renders light properties.
 */
export {};
