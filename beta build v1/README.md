# Beta Build V1 — frozen 2026-07-05

The first feature-complete build (Phases 0-3): object mode, edit mode
(extrude/inset/loop cut/box select), save/load, OBJ import/export, shading
modes, splash + F1 help. 126 unit tests. Also deployed at
https://raymundo302.github.io/vibe-blender/

Run `./launch.sh` to open it for filming (serves on port 5310, separate from
the dev server on 5199, so the frozen and live versions can run side by side).
Source snapshot: git tag `beta-build-v1`.
