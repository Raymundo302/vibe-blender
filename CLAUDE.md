# Vibe Coded Blender

## Project Goal
Build a Blender-style 3D modeling app from scratch, documented as a YouTube video ("can AI recreate Blender?"). The interesting deliverable is both the app AND the story of how it was built with AI orchestration.

## Orchestration Strategy (decided 2026-07-02; reaffirmed by Ray 2026-07-08 — applies to Use & Refine batches too, not just phases: Fable architects + specs, Opus subagents do the heavy implementation lifting)
- **Fable 5 (main session)** = architect + project manager. Keeps for itself: overall architecture, core mesh data structure, undo/redo system, viewport/camera math, integration review — anything where a wrong early decision poisons downstream work.
- **Opus subagents** = implementation workers for well-specified subtasks: UI panels, individual modeling tools (once the tool interface exists), importers/exporters, shortcut handling. Spawn via Agent tool with `model: "opus"`. Opus is half Fable's token price; workers burn ~95% of tokens, so this is where the savings are.
- **Sequencing rule:** Fable designs/builds core interfaces FIRST, then writes subtask specs referencing real existing code (not imagined shapes). Specs include: exact file paths, interfaces to conform to, out-of-scope list, acceptance criteria the worker can self-check.
- **Worker escape hatch:** every subagent prompt includes "if the spec is ambiguous or wrong, stop and report rather than improvising."
- **Verify loop:** Workflow tool per phase — implement batch → verify each against acceptance criteria → collect failures → re-dispatch fixes. Fable reads results between phases and decides the next workflow. Run mechanical stages at low effort, verify/review at high effort.

## State Management
- Task registry lives IN THIS REPO (human decision: git is the source of truth, not Notion). Use `PLAN.md` / `tasks/` with per-module status: pending / built / verified / failed. Every subagent marks its own completion; verify passes update statuses.
- Any session can read the state and know what's next — this is what makes the build resumable.

## Safe-Stop / Credit Exhaustion Plan
- No built-in pause/resume on usage limits. Approximation:
  1. Workflow runs journal completed agent calls → `resumeFromRunId` replays finished work from cache at zero cost after an interruption.
  2. Budget-cap each workflow phase (`budget.remaining()`) so phases end cleanly with state written, instead of dying mid-task.
  3. Optional: scheduled job (/schedule or /loop) fires every few hours with "read task state, continue the build" — runs during exhausted limits just fail; first run after reset resumes from disk state.

## Notion (human notes only — NOT project state)
- Notion MCP server connected at user scope (authenticated 2026-07-02).
- At natural checkpoints (phase complete, blocker hit), write a short plain-language update to the user's Notion project notes: where the build stands, decisions made and why, anything waiting on his input.
- Never put operational state (task statuses, specs) in Notion.

## Launching the App for Ray
When Ray asks to open / load up Vibe Blender, he means a NEW standalone window with no address bar — only the app. Launch: start the dev server (`npm run dev -- --port 5199 --host localhost`, the port the e2e harness also expects), then `google-chrome-stable --app=http://localhost:5199 &`. Do NOT use `firefox --kiosk` — if Firefox is already running it just opens a tab in the existing window instead.

## Safety Note
User runs bypass-permissions mode. Keep ALL work inside this directory; verify stages should check for out-of-scope file changes.

## Tech Stack (decided 2026-07-02)
- **TypeScript + raw WebGL2, no engine** (no Three.js). Rationale: "from scratch" credibility for the video narrative, fast refresh-the-browser iteration for parallel Opus workers, demoable via a link in the video description. We hand-write the math library, shaders, camera/projection, and mesh system.

## Avatar → separate project (moved 2026-07-02)
- Fable's avatar (cyborg fox, 25-expression library, talk cycle, performance pipeline plans) is now its own project: **`~/Fable Fox/`** — see that folder's CLAUDE.md for all character/PixelLab details.
- This video USES the avatar; it doesn't own it. Reference assets from `~/Fable Fox/expressions/` and `~/Fable Fox/animations/`.
- Avatar process log lives in its own Notion page: "Building my AI avatar".

## Video Production (planning in Notion)
- The video plan (structure, entertainment strategy, how to present Fable as a character) lives in the Notion page **"Vibe Coding Blender"** — this is a human-notes doc, not operational state.
- Creative direction: entertaining first, but grounded in real math — teach how 3D software works "at the moment of need" (each concept introduced when it fixes a visible bug or unlocks a feature), not as lectures.

## Status
- [x] Strategy decided, folder + git repo created
- [x] Tech stack decision — TypeScript + raw WebGL2, no engine (2026-07-02)
- [x] Video plan drafted in Notion ("Vibe Coding Blender") — avatar decided, built, split into ~/Fable Fox (2026-07-02)
- [x] Architect/worker workflow validated on the Fable Fox pixel optimizer — 6 spec→implement→verify Opus increments, zero deviations (2026-07-02)
- [x] Game plan generated — architecture decisions (modal operators, BMesh-lite, color-ID picking, snapshot undo, DOM panels, matcap), module map, 4-phase task registry in PLAN.md (2026-07-05)
- [x] Phase 0 core built & verified — viewport with grid/axes, matcap cube, orbit/pan/zoom, click-select with orange outline, modal G-translate with axis locks, undo/redo. 18 unit tests + 12-check headless e2e (`node e2e/smoke.mjs`). Run with `npm run dev`. (2026-07-05)
- [x] Phase 1 object mode built & verified — 5 primitives + PRIMITIVES registry, R/S modal operators (axis locks, numeric input), translate gizmo (picking-based handles), outliner, properties panel (editable transform, euler↔quat), Shift-A/Shift-D/X, header bar + Blender-dark theme. Workflow `p1-object-mode`: 7× Opus implement → adversarial verify; 1 fix round (P1-4 sidebar shrank canvas → floated panel). 55 unit tests, 12/12 e2e. (2026-07-05)
- [x] Phase 2 edit mode built & verified — Tab/1-2-3 edit-mode core + cage overlay (Fable), element picking, G/R/S on elements, extrude, inset, delete/merge menu, box select + invert (Opus workflow `p2-edit-mode`, 6/6 verified, 0 fix rounds), loop cut with quad-strip walk + yellow preview (Fable). 107 unit tests, full `e2e/edit.mjs` suite. (2026-07-05)
- [x] Phase 3 shipped — scene save/load (versioned JSON, Ctrl+S/O), OBJ import/export, Z-cycled shading (matcap/wireframe/studio), splash + F1 shortcut overlay (Opus workflow `p3-ship-demo`, 4/4 verified, 0 fix rounds). Deployed by Fable to **https://raymundo302.github.io/vibe-blender/** (repo: github.com/Raymundo302/vibe-blender, Pages from gh-pages; re-deploy = `npm run build` + force-push dist to gh-pages). 126 unit tests, 3 e2e suites. (2026-07-05)
- [x] Beta Build V1 frozen for filming — `beta build v1/` folder with `./launch.sh` (port 5310), git tag `beta-build-v1` (2026-07-05)
- [x] Night session (2026-07-05→06), Phases 4-6, 23 Opus tasks all verified, 0 failures: **workspaces** (Layout/Modeling tabs, switchable/fullscreenable/resizable areas, viewport swap), **modifier stack** (tabbed Properties editor with Object + Modifier tabs; Mirror, Array, Catmull-Clark Subsurf; apply/undo; scene format v2), **modeling tools** (dissolve, bridge Ctrl+E, bevel Ctrl+B, fill F, subdivide Ctrl+D, frame ., Delete-key alias), **polish** (shade smooth, N-panel, per-object colors, autosave+restore, proportional editing O, full shortcut audit). 202 unit tests, 4 e2e suites, deployed. Tag `night-build-2026-07-06`.
- [x] Phase 7 built & verified — join (Ctrl+J), separate (P), edge slide (GG), grid snapping (Shift+Tab toggle + Ctrl-hold invert, magnet chip), edit-mode duplicate (Shift+D rides Move). Workflow `p7-object-precision`: 4× Opus implement → adversarial verify, 1 fix round (P7-2 test coverage). Also fixed e2e harness silent-abort flaw found by the P7-3 verifier (exceptions mid-suite used to print "All passed"; `until()` now retries during navigation). 234 unit tests, 4 e2e suites, deployed. Tag `p7-build-2026-07-06`. (2026-07-06)
- [x] Beta Build V2 frozen for filming — `beta build v2/` with `./launch.sh` (port 5320), git tag `beta-build-v2` (2026-07-06)
- [x] Phase 8 built & verified — camera/lights/materials/render engine (2026-07-06, while Ray was at the dentist). Fable core: object kinds (light/camera with data payloads, empty meshes), scene material library, **Rendered viewport mode** (forward Cook-Torrance GGX, 8 lights: point/sun/spot), billboard icons + picking for non-mesh objects, Shift+A Light/Camera, kind-aware duplicate. Opus workflow `p8-render-engine` (5 implement + 5 adversarial verify): Light tab (P8-1), Material tab (P8-3), **F12 progressive path tracer** — Web Worker, BVH, seeded RNG, render window with Save PNG (P8-4), camera frustums + Numpad0 view-through-camera + Camera tab (P8-2), scene format v3 with materials/lights/cameras + outliner kind glyphs + join guard (P8-5). Fix round by Fable: activeCamera serialized as objects INDEX (ids never hit the file → byte-identical round trips even with id gaps), join skips non-mesh without touching selection, ship.mjs v3 literals. 290 unit tests, 9 e2e suites, deployed. Tag `p8-build-2026-07-06`.
- [x] Beta Build V3 frozen — `beta build v3/` with `./launch.sh` (port 5330), git tag `beta-build-v3`; topbar 🎬 Render button added (browsers eat F12) (2026-07-06)
- [x] Donut gap analysis — transcripts of Blender Guru's 2026 donut series (8 parts) mined by 8 parallel agents; gap report in `DONUT.md`, transcripts in `research/` (2026-07-06)
- [x] Phase 9 built & verified — THE DONUT TOOLKIT (2026-07-06 evening). Fable core: ModifierContext (object-referencing modifiers with cycle guard + dep-aware caching, refs serialized as indices), edge creases, per-face tints (GPU plumbed), SSS material fields, 'object' modifier field kind. Opus workflows (`p9-donut-batch-a`, `-b`, `-close`; 8 tasks, every one adversarially verified): Solidify + creased Catmull-Clark + merge-by-distance (P9-1), Shrinkwrap (P9-2), adjust-last-operation panel + parametric primitives + Circle (P9-3), tracer soft shadows/SSS/DoF + light radius (P9-4), Scatter modifier with per-instance pastel tints (P9-5), Alt+click loop select + X-ray click-through + Shift+N + Shift+E crease + Ctrl+Alt+Numpad0 camera-to-view (P9-6), sculpt-lite Inflate/Grab (P9-7), **the donut dry run** `e2e/donut.mjs` — full donut through public entry points, 36 checks, hero path-traced render (P9-8; findings in `research/DONUT-RUN.md`, scene in `research/donut.vibe.json`). Fix round: ApplyModifierCommand ctx threading + copyFrom(self) guard, tracer honors faceTints via derived materials. Infra: harness `t.reload()` (killed the "ship flake" — a stale-context race), synchronous chrome-profile cleanup (was leaking ~100MB/run until /tmp filled). 385 unit tests, 17 e2e suites.
- [x] Beta Build V4 frozen — "the donut build": `beta build v4/` with `./launch.sh` (port 5340) opening straight into the bundled donut scene via the new `?scene=` deep link; tag `beta-build-v4` (2026-07-06)
- [x] Phase 10 built & verified — collections (outliner groups + M popup), theme system (8 themes: Claude/Default + six 90s from Ray's reference; 🎨 picker; viewport colors theme-driven), 🌍 World (flat/gradient/HDRI → rendered backdrop + tracer sky), camera Lock-to-View + passepartout. Workflow `p10-collections-themes-world`, 4/4 verified, 0 fix rounds. Tag `p10-build-2026-07-07`. (2026-07-07)
- [x] Phase 11 built & verified — UVs, THE NIGHT BUILD (2026-07-06→07). F11-1 core: per-corner mesh.uvs + seams, material texKind/texDataUrl, GPU attr 3 + tracer triUV. Workers (workflows `p11-uv-night` + `p11-uv-modifiers`, 5/5 verified): Ctrl+E edge menu (Mark/Clear Seam + Bridge folded in) + U menu (Unwrap = seam islands + Tutte relaxation + shelf pack; Smart UV Project; Project From View), UV Editor workspace pane (own canvas, island select, G/R/S, 3D sync), checker/image textures in Rendered + tracer, modifiers preserve UVs (bilinear per-face UV subdivision in subsurf). Dry run `e2e/p11-uv-dryrun.mjs`: donut icing seamed→unwrapped (6 islands)→checkered on the LIVE modifier stack, F12-proven; findings in `research/UV-RUN.md`, scene `research/donut-uv.vibe.json`. Fixture churn fixed via frozen `e2e/fixtures/donut-p9-frozen.vibe.json`. 467 unit tests, 25 e2e suites. Tag `p11-uv-2026-07-07`.
- [x] Beta Build V5 frozen — "the UV night build": `beta build v5/` (port 5350) opening into the UV'd donut; tag `beta-build-v5` (2026-07-07)
- [x] Phases 12–15 planned (2026-07-07, night-build queue in PLAN.md): **P12** 3D cursor + Shift+S snap pie + overlay toggles (grid/origins/light icons) + parenting; **P13** bump/normal + roughness maps + Image Viewer pane; **P14** node-based shader editor (TS evaluator, A14); **P15** animation (FCurves, timeline pane, keyframes, A15). Sequenced 12→13→14→15; P14/P15 each ~Phase-8-sized, budget-cap per phase.
- [x] Phase 12 built & verified — parenting (Ctrl+P/Alt+P, drag-to-parent outliner, world-matrix refactor of EVERY consumer, sceneJson v4), 3D cursor (Shift+RMB place, add-at-cursor, pivot mode, Shift+C), Shift+S snap pie (generic PieMenu component), Overlays dropdown + origin dots + 💡 lights toggle. Workflow `p12-cursor-pies-outliner`, 3/3 adversarially verified, 0 fix rounds. (2026-07-08 night)
- [x] Phase 13 built & verified — normal/bump + roughness/metallic maps in Rendered (derivative-TBN GLSL) AND tracer (real tangent frames, bit-stable no-map path), Image Viewer pane (render result + material slots, zoom/pan/pixel readout), material tab Maps section (RAW decode caches). sceneJson v5. Workflow `p13-maps-imageviewer`, 3/3 verified, 0 fix rounds. (2026-07-08 night)
- [x] Phase 14 built & verified — NODE SHADER EDITOR. Fable core: graph model/evaluator/registry (cycle-proof, memoized), bake-to-texture bridge for Rendered (A14: one evaluator, two consumers), imageCache, sceneJson v6. Workers: Shader Editor pane (generic over NodeDefs, GraphEditCommand snapshot undo), tracer hook (bit-identical non-node path), nodes: Checker/Noise/MixColor/ImageTexture/ColorRamp/Math. ONE verify failure tonight: imageTexture sampled v-flipped vs the app convention — architect fix also caught the same bug in bake.ts. Dry run: donut icing shaded procedurally, hero at research/donut-nodes-hero.png, findings research/NODES-RUN.md (top gap: generated texture coords). (2026-07-08 night)
- [x] F15-1 animation core — FCurves (constant/linear/bezier auto-Hermite), channelPath system (transforms euler-bridged + light/camera/material), parent-aware sampler, I-key LocRotScale, Insert/DeleteKeys commands, timeline fields, sceneJson v7 (loads land posed at frameCurrent). (2026-07-08 night)
- [x] Phase 15 built & verified — ANIMATION. Fable core F15-1 (FCurves constant/linear/bezier, channelPaths incl. light/camera/material, parent-aware sampler, I-key, sceneJson v7) + F16-1 precision fix (only keyed transform components rebuild — kills the euler↔quat settle). Workers: Timeline pane (scrub/playback/diamonds/Space — ONE verify fail: Space co-fired with shader-pane pan; architect fix), I keying menu (I,I confirms), key select/move/delete + auto-key ⏺, light/material ● key buttons + the camera fly-through dry run (research/ANIM-RUN.md, research/donut-flythrough.vibe.json). (2026-07-08 night)
- [x] Phase 16 built & verified — 🎞 RENDER ANIMATION (Ctrl+F12: frame loop → WebM via MediaRecorder or store-only PNG zip w/ own CRC32 writer; progress+cancel; state restored), Texture Coordinate node (Generated coords via tracer triGen — procedural shading without a UV map; bake falls back to UV, documented), Map Range + noise contrast, dope-sheet-lite (per-channel timeline rows + per-key interp picker), quick-wins batch (theme toggle-close, M→New Collection single undo, lock-to-view continuity, proportional radius, RGBE .hdr parser). Workflow 4/4 verified, 0 fix rounds. 653 unit tests, 33 e2e suites, deployed. Tags `night-build-2026-07-08`, `beta-build-v6`. (2026-07-08)
- [x] Beta Build V6 frozen — "the animation build": `beta build v6/` (port 5360) opening into the keyed donut fly-through; tag `beta-build-v6` (2026-07-08)
- [x] Use & Refine batch 1 (2026-07-08, day session): **viewport shadows + Z-UP CONVERSION.** (1) Real-time shadow maps in Rendered mode — sun (ortho, scene-bounds-fitted), spots (perspective, cone-fitted; up to 4 casters, 1024² w/ hardware PCF via `sampler2DShadow`) AND point lights (cube maps: 2 slots, 512² faces, 6 depth renders each, `samplerCubeShadow` w/ major-axis distance → clip-depth reference, distance-scaled world bias). (2) Selection outline anti-aliased (averaged circular kernel + soft alpha in outlinePass). (3) Sun/spot aim arrows (lightDirPass, rides the icons overlay toggle). (4) Locked-axis indicator: G/R/S with X/Y/Z lock keeps that axis's gizmo arrow + a Blender-style guide line (Operator.axisIndicator() hook → InputManager → renderer; guide is CPU-clamped against the near plane — SwiftShader DROPS lines with an endpoint behind the eye instead of clipping). (5) **World switched Y-up → Z-up** (Blender convention): OrbitCamera yaw-around-Z, grid/plane/cylinder/torus/circle primitives re-axed, sky gradients + equirect on dz, sceneJson **v8** with auto-migration (roots premultiplied Rx90, location fcurves channel-swapped, rotation fcurves recomposed per key; orbit yaw/pitch carry over — verified against the real v3 donut fixture + v7 fly-through). 665 unit tests, all e2e suites converted (5 parallel Opus workers) & passing. **e2e harness gotcha (CLOSED 2026-07-09 — `E2E_PORT` env var):** fixed port 9222 meant a stale headless Chrome silently hijacked runs with polluted localStorage; set a unique E2E_PORT for concurrent runs. Also: e2e center-pixel reads must avoid the object origin — the gizmo arrows converge there.
- [x] Use & Refine batch 2 (2026-07-08, day session): **viewport shading dropdown + AO + wire toggles.** Shading mode moved from the topbar chip to a dropdown on the RIGHT of the 3D Viewport's area header (new `EditorInstance.headerExtra` slot in workspace.ts; `src/ui/shadingMenu.ts`; Z-cycle stays in sync). Options in the dropdown (persisted via `src/render/shadePrefs.ts`, exposed as `__app.shadePrefs`): **Ambient Occlusion** (SSAO: depth prepass → 12-sample hemisphere kernel w/ depth-derivative normals → box blur, multiplied into matcap/studio/rendered; `src/render/passes/aoPass.ts`) with **Radius + Strength tuner sliders** under the checkbox (persisted numerics, clamped on load), **Wireframe** overlay (edges over shaded modes, depth-tested w/ NDC bias), **Hidden Line** (wireframe mode: colorMask-off depth prime → backfacing/occluded wires culled). Gotchas: e2e `t.evaluate` does NOT await promises (async IIFEs are fire-and-forget — use `__app` handles for sync state); two flex `margin-left:auto` siblings SPLIT the free space (header-extra + fullscreen btn needed an `:empty` override); wireframe-pixel checks must count a region — single pixels are dark-on-dark ambiguous vs the grid. 672 unit tests, new `e2e/shading.mjs` suite.
- [x] AO research + GTAO rebuild (2026-07-08, evening): Ray reported persistent banding in the SSAO (his artifact shot: `research/ao-v1-artifacts.png`). Opus research worker surveyed the field → `research/AO-RESEARCH.md` (engines ship GTAO-family horizon AO; our banding was STRUCTURAL: 16 samples = 17 discrete levels + bilateral blur dying at grazing angles + 8-bit posterization + tiled-noise stipple). Opus implementation worker rebuilt per blueprint: **GTAO-lite** (3 slices × 6 steps, closed-form cosine arc), **Interleaved Gradient Noise**, **R8+dither / R16F** target (additive Framebuffer format support), **plane-relative bilateral** denoise. e2e gained a **banding detector** (distinct-level/max-run histogram over the contact-shadow ramp) that provably FAILED v2 (maxRun 9) and passes GTAO (maxRun 2). 671 unit tests, 25 shading e2e checks. Process lesson for the video: v1+v2 passed numeric checks and failed eyes — screenshots-as-verification are mandatory for visual features. Research-process notes added to the Notion page for the video. **Beta Build V7** (`beta build v7/`, port 5370) freezes the v1 BANDED AO for before/after filming (bundle fingerprint: v1 sine-hash constant present; main build has GTAO/IGN).
- [x] GTAO grazing-angle fix (2026-07-08, late): Ray's second artifact report (regular parallel stripes on floors at shallow camera angles — `research/ao-v3-artifacts.png`) root-caused by an Opus debug worker: **depth-precision noise in the horizon march at grazing incidence** — quantization slides each reconstructed tap along its own view ray, so elevation-bias rejection can't separate false floor taps from real contact occluders. Fix: **ray-relative coplanarity gate** — for each tap, k = dot(P,N)/dot(sP,N) is exactly 1 for coplanar taps at ANY view angle (well-conditioned at grazing); reject |k−1| < 0.04. Open-floor false darkening at grazing went to literally ZERO (p95 |off−on| 6.8 → 0.0) with contact shadows preserved. e2e gained a grazing-pitch flat-floor + row-oscillation stripe regression (fails pre-fix, passes post). Files: aoPass.ts + shading.mjs only. LESSON: viewport visual features must be regression-tested across CAMERA ANGLES, not just the default view — and per-pixel difference fields (off−on, amplified) are the diagnostic tool that found this.
- [x] Beta Build V8 frozen (2026-07-08, end of day session): "the second AO fix attempt" — GTAO + coplanarity gate, port 5380. **Ray still reports the AO "not that good" on his Vega 7 after TWO Opus fix attempts that both passed all rig verification** (numeric + SwiftShader screenshots). Betas V7 (banded v1, port 5370) and V8 bracket the AO saga for the video; Ray notes if credits run out here it's a natural video ending.
- [x] **THE AO BUG — SOLVED BY FABLE ON THE REAL GPU (2026-07-08 evening).** Root cause of the streaks both Opus rounds missed: **`sampler2D` defaults to `lowp` in ES fragment shaders and radeonsi honors it** — the AO pass sampled its depth prepass through an fp16 path, so reconstructed floor points sat centimetres off-plane in row bands → the horizontal streak/blotch artifacts on Ray's Vega 7. SwiftShader samples fp32 regardless of precision qualifiers, so the e2e rig **could not ever** reproduce it. Fix: `uniform highp sampler2D` for depth/normal/src in GTAO + bilateral (aoPass.ts, one-word-per-declaration). Method that found it (keep for the video, it's a great beat): headless Chrome **`--enable-gpu` gives the real Vega 7 in the exact e2e pipeline** (`ANGLE (AMD, radeonsi renoir)`); saved Ray's live scene via Hyprland `sendshortcut` Ctrl+S; dumped per-stage AO buffers (raw GTAO vs blur) on both backends; bisected march direction (horizontal clean / vertical streaked); measured winning-tap |k−1| ≈ 0.01–0.03 → 5–15cm phantom elevations → fp16-sized depth error → lowp sampler. Infra: `E2E_GPU=1 node e2e/<suite>.mjs` runs any suite on the real GPU (harness.mjs); shading.mjs passes on BOTH backends incl. banding + grazing-stripe regressions. Repro scene: `research/compare-blender-2026-07-08.vibe.json` (Ray's cube-with-hole on plane, saved from his live session); capture-script pattern in scratchpad notes, stage dumps in `research/ao-stage-*`, before/after in `research/ao-real-gpu-*` / `ao-fixed-gpu-*`. Commit `4ab48a2`. **Remaining AO quality gap vs real Blender: graininess/stipple of the contact shadow (3 slices × 6 steps IGN + 5×5 blur) — a tuning/quality knob, not a bug; revisit only if Ray still dislikes the look on real scenes.** → CLOSED same evening, see next entry.
- [x] **AO QUALITY REBUILD → BLENDER-MATCHED (2026-07-08, late evening; Ray-directed "keep going till it looks like Blender").** Grain gone, look calibrated against Ray's EEVEE screenshots of the same cube-with-hole scene (side-by-sides: `research/ao-q*-contact-compare.png`). What shipped (commits `9c2f7cf`, `2a249e6`): (1) **AO chain at HALF canvas resolution** (EEVEE-1:2-style) — GTAO + denoise run at W/2×H/2 into a LINEAR-filtered final target (`Framebuffer` gained a `linear` flag; consumers sample normalized uv so the upsample is transparent); (2) STEPS 6→8; (3) denoise = **two separable 13-tap plane-aware Gaussian passes** (~24×24 canvas-px footprint) replacing the 5×5 bilateral — this is what melted the IGN stipple; (4) look: falloff starts at 0.5R, contrast pow 1.15, **defaults radius 1.2 / strength 0.9** (calibrated vs Blender's wide, airy, light contact shadow); (5) `STORAGE_KEY` → `vibe-shading-v2` so old stored tuner values don't shadow the new defaults; (6) banding e2e check made **ratio-based** (distinct ≥ 0.8·active) so it pins smoothness, not ramp reach; (7) **boot deep links `?shading=<mode>&ao=1`** (main.ts) for captures/demos. Verified: 671 units, shading e2e on SwiftShader AND Vega 7, donut fixture on real GPU. Residual (fine to leave): our *studio light rig* fills inward-facing walls darker than Blender's studiolight — a lighting-rig gap, not AO.
- [x] **AO round 3 — wobbly edges, Samples option, THE DIAMOND (2026-07-08, night).** Ray's reports, all fixed & committed (`fd238a8`, `f7e5fa5`): (1) **wobbly silhouette edges** = half-res AO through plain LINEAR upsampling → new **depth-aware upsample pass** (4 half-res texels, bilinear × plane-consistency vs FULL-res depth/normals; before/after `research/ao-wobble-before-after.png`); (2) **Samples slider** in the shading dropdown (16–96 spp, default 48; GTAO loop bounds are now uniforms via `sampleBudget()`); (3) **boomerang shadow** (cube's ambient diamond faded toward the far corner, vanished at grazing pitch) — TWO root causes: **unnormalized Jimenez slice arcs** (an open slice integrates to cos g + g·sin g = π/2 at grazing, burying occlusion before the visibility clamp → now normalized per slice by its own open-arc max) and **back-facing occluders absent from the depth buffer** (→ slab thickness: candidate occluder = point on the tap's view ray closest to the center, clamped within one radius behind the visible surface, EEVEE-style). e2e additions: grazing-diamond regression (near AND far corner creases must darken; far probe placed diagonally outside the corner — the crease straight beyond hides behind the cube), crease-probe scene now polls for the floor object (t.evaluate doesn't await async IIFEs — was a deterministic flake). Numeric probes (grazing, real GPU): near corner 103, edges 58–69, far corner 149, open floor 188 — corners lighter than edge midpoints is PHYSICALLY correct (quarter- vs half-plane of wall). Debug technique that cracked it: "any-tap-fired" shader diagnostic + swapping the arc integral for plain horizon visibility, both at Ray's exact framing via `__app.camera` scripting.
- [x] **AO round 4 — view-stability + THE FLOATING CUBE (2026-07-08, late night; commit `2245675`).** Ray: "ambient shadows don't move when you rotate a camera." Fixed the camera-coupled shader bugs: (1) **radius falloff corrupted the cosine domain** — mix(-1, cos, falloff) pushed near-perpendicular horizons backward, deleting the strongest occlusion at contact creases (now softens the horizon ANGLE toward open); (2) **bilateral filters get a normal-agreement term** — plane distance can't separate two surfaces near their meeting line, so wall AO bled over floor contact shadows for a full blur radius; (3) off-screen marches/blur taps contribute nothing (edge-clamped depth made shadows swim with framing); (4) coplanar band 0.04→0.015 (pre-highp relic, ate crease occlusion). **Plot twist: the comparison scene's cube was FLOATING 0.274 units above the plane** — the "glow underneath / detached shadow" at grazing was CORRECT rendering; scene fixed (cube z=1.0, rests). LESSON FOR THE VIDEO: check the scene geometry before hunting renderer bugs — hours of shader forensics traced to a hovering cube. Tooling built this session (all in scratchpad, patterns in research/): orbit-sweep contact sheet (8 yaws × 3 pitches, one PNG), AO-on/off screenshot differ, in-shader diagnostic maps rendered as the AO term. Orbit sweep clean at all 24 angles; suites green both backends.
- [x] **Wire overlay corner stubs fixed (2026-07-08, night; commit `c74c6d8`).** Hidden edges poked through at shared silhouette corners: at the corner vertex a back edge's depth equals the surface's, so the overlay's 0.002 NDC bias let a short stub win the depth test. Fix = Blender-style orientation culling: `meshToGpu` edge buffer now carries BOTH adjacent faces' normals per edge vertex (locations 1,2 on the edges VAO; boundary edges repeat n1); `wirePass` `u_hideBack` pushes a vertex to far depth when both faces point away (per-object `u_normalMat`). Applies to overlay + Hidden Line; classic see-through wireframe keeps all edges. Known pre-existing (flagged, not fixed): grid-vs-floor-plane z-fight dashes at z=0 (Blender fades its grid under geometry — punch list), and n-gon triangulation diagonals around holes are real topology, same as Blender.
- **AO/debugging playbook distilled from the 2026-07-08 saga (for future viewport work):** (1) verify on the REAL GPU — headless Chrome `--enable-gpu` in the exact e2e pipeline; SwiftShader hides driver-precision bugs (lowp samplers!) and passes rigs that fail eyes; (2) separate SUSPECTS with control sweeps — the AO-off orbit sweep instantly re-attributed "black faces" from AO to the studio light rig; (3) orbit-sweep contact sheets (8 yaws × 3 pitches, one image) catch angle-dependent artifacts single-view captures miss; (4) when a shadow "misbehaves", check the SCENE FIRST — hours of shader forensics ended at a cube floating 0.274 above the plane; (5) in-shader diagnostic maps (output the suspect scalar as the AO term, read it off a screenshot) beat readPixels plumbing; (6) screenshot pairs (feature on/off) + Python pixel profiles are the fastest ground truth; Ray's screenshots land in `~/Screenshots/` (waybar 📷 button).
- [x] **OBJECT AO MODE (2026-07-09)** — Ray's `~/AO-Prototype/ao-hybrid.html` technique integrated as a second AO estimator: shading dropdown → AO Mode **Screen (GTAO) / Object (SDF)** + a 6-entry Method menu (Baseline/Dithered/Cone/Hemisphere/Exp-weighted/Supersample — his `aoLive()` ported verbatim). Architecture: per-object **voxel SDFs** (`src/render/sdf.ts`, 48³ local-space: narrow-band exact point-triangle + chamfer sweep + flood-fill sign, R8-encoded) in a 3D texture atlas (`src/render/sdfAtlas.ts`, 32 slots, cached by the gpuMesh version key → transforms free, only edits re-voxelize, 150 ms rebuild throttle); fullscreen pass in aoPass.ts marches the WORLD-space field (camera-independent — e2e proves crease spread 19/765 across 3 yaws) and reuses the GTAO half-res denoise+upsample chain. **Donut-scene lessons:** (1) Scatter's sprinkle cloud = dozens of sub-voxel islands in ONE mesh → its "SDF" is noisy near-zero garbage that blackened sprinkles + blotched the floor; fixed by a sparse-cloud occluder-exclusion heuristic (tri-area/box-area < 0.05 → casts no AO, still receives) AND a half-voxel debias in the shader's map(). (2) Sprinkles live in the ICING's evaluated mesh (Scatter modifier), not the Sprinkle object — probe the evaluated mesh, not the outliner. Deep links `?aomode=object&aomethod=N`. **Method menu trimmed + gain-calibrated same day (Ray's pass):** Dithered/Cone cut on looks, Supersample ×4 cut for lag → keepers are **Baseline (0, default — Ray's prototype favorite), Hemisphere (1), Exp-weighted (2)**, each with a hardcoded shader gain (`GAIN_BASE 1.0 / GAIN_HEMI 0.57 / GAIN_EXP 1.44` in aoPass.ts) calibrated on the cube-on-floor rig so all three AND Screen/GTAO hit the same crease darkness at identical sliders (probe spread 3/765; e2e pins it at <30). Falloff CHARACTER still differs by design (hemi reaches broader/darkens walls). Stored aoMethod indices from the 6-method era clamp to 0..2 on load. **Defaults (Ray, same day): aoMode='object' + Baseline, radius 0.3, strength 1, samples 48** — storage key bumped to `vibe-shading-v3`; the AO checkbox itself still defaults off; the GTAO e2e sections pin `aoMode='screen'` explicitly. Workers: UI selects + 15 e2e checks (2 Opus, 0 fix rounds). 678 units, shading suite green both backends. Screenshots: `research/objao-donut-{off,cone,hemi,gtao}.png`.
- [x] **Hidden-edge leak fixed (2026-07-09).** Ray: "geometry not hiding mesh edges behind it" — wire overlay / edit cage / loop-cut preview / element-pick all used a CONSTANT NDC depth bias (0.002/0.001); NDC compresses with distance, so anything within ~0.01·z² world units behind a surface leaked through (the torus sits ~0.1 under the icing → its whole wireframe drew through the icing, X-ray look with X-ray off). Fix: bias = FRACTION of view distance (`viewPos.xyz *= 1-k`, radial → screen pos unchanged) + few-ULP epsilon, in wirePass + editOverlayPass + elementPickPass (pick must mirror the visible cage). A/B: `research/wire-ab-{before,after}.png`. **Debug lesson (cost an hour): verify the repro scene before the renderer — `makeCube(n)` takes a HALF-extent, so my "fully occluded" back cube was actually bigger than the front one and every "leak" in the synthetic rig was legitimate rendering; the real leak only showed on the donut fixture.** Suites green both backends.
- [x] **Workspace split/merge + docked Timeline (2026-07-09).** Areas divide & merge: ⋮ header menu (Split Horizontal / Split Vertical / Close Area) + Blender's corner-drag on each area's top-right corner (drag IN = split — left→new column, down→stacked; drag OUT = consume the up/right neighbor; right-merge only into single-area columns — the column-grid model can't nest). splitArea/closeArea/mergeArea mutate the active WorkspaceConfig + rebuild via the switchTo machinery; viewport singleton parks/returns; last area refuses to close. Default Layout now docks a **Timeline under the viewport (80/20)**; workspace storage key → `vibe-blender-workspaces-v2`. ⚠️ The docked pane means TWO timeline canvases + a shorter viewport — e2e suites must scope `.timeline-*` queries via `window.__timeline.canvas` and never assume the cube sits at canvas center (broke shading's single-pixel wire probe → darkest-of-strip; p11-textures' center strip → cube-anchored; p8-scene/ship CURRENT_FORMAT_VERSION consts). Opus worker built it, 0 fix rounds.
- [x] **ANIMATION: easing + Graph Editor batch (2026-07-09).** Core (Fable): `fcurve.ts` — eased interp families sine/quad/cubic/quart/back/bounce/elastic (Penner) with per-key `easing` auto/in/out/inout (auto = Blender's rule: dynamic families ease OUT), and FREE bezier handles (`handleMode` auto|free, `hl`/`hr` [frames,value] offsets; free spans = true 2D cubic w/ Newton x-solve + handle-x clamped into the span; pure-auto spans keep the old Hermite bit-exactly). `insertKey`/MoveKeys thread extras (moves never strip easing/handles). New commands: SetKeyEasingCommand, EditCurveKeysCommand (whole-curve snapshot capture(name, targets, mutate) — the Graph Editor's undo backbone). **sceneJson v9**: optional 4th key element {e, hm, hl, hr}. Workers (Opus ×2, 0 fix rounds): Timeline interp picker → all 10 modes (`INTERP_MODES`) + Easing select (`EASING_MODES`, enabled per `EASED_INTERPS`), **single-playback-clock fix** (module-scoped owner — two Timeline panes no longer double playback speed; punch-list item CLOSED); **Graph Editor pane** (`src/ui/graphEditor.ts`, editor type 'graph') — per-channel colored F-curves (x/y/z = R/G/B, sampled per pixel column via evalFCurve), frame ruler + value grid, wheel zoom (Ctrl = y-only) / MMB pan / Home+⌂ fit, draggable key diamonds (frame+value, integer-snapped frames) and bezier handle squares (drag → free mode), `window.__graph` e2e handle, own suite `e2e/graph.mjs`. 691 units.
- [x] **Timeline navigation (2026-07-09, Ray-requested).** Pane-local view window decoupled from the scene frame range (editing Start/End never moves the view; the in-range span draws a subtle lighter band). Mouse-anchored wheel zoom (frame under cursor stays put), MMB pan, '.' (pane hovered) = zoom-to-selected-keys / fit-range. Recursive-adaptive ruler grid: majors from the 1-5-10-50-100 ladder at ≥60px, faint minors at ⅕ (exported pure `majorGridStep`). Debug: `__timeline.view()/setView()/gridSteps()`. **Infra: e2e harness debug port is now `E2E_PORT`-overridable** — the "stale 9222 hijacks parallel e2e runs" punch item is CLOSED (three parallel workers collided on it the same day; the sweep-with-commit background job also raced worker WIP — lesson: don't `git add -A` from a background job while workers run).
- [x] **Primitives ship UV-unwrapped + Displace modifier (2026-07-09, 2 Opus workers).** All makers in `primitives.ts` carry Blender-conventional default unwraps (cube = cross layout, uv-sphere = equirect w/ seam+pole rules, cylinder = tube+cap circles, torus = grid, ico = spherical projection w/ wrap fix, plane/circle planar) — per-face-corner storage makes seams free; topology untouched. Tests needing UV-less meshes now `uvs.clear()` explicitly. NEW `displace` modifier (`src/core/modifiers/displace.ts`): vertex-normal displacement `strength·(tex−midlevel)`, tex = deterministic 3D fbm value noise (scale/detail/seed) or 'none'; preserves all attributes; modifier schema gained a generic `'select'` field kind. e2e/displace.mjs.
- [x] **PUNCH-LIST CLEAROUT (2026-07-09, 6 parallel Opus workers + integration).** (1) **Knife tool** — K in edit mode, screen-space polyline cut (ops/knife.ts pure geometry + modal operator w/ SVG preview; no cut-through/mid-face points, documented). (2) **Scatter align-to-normal** — 'align' param, local +Z along face normal + yaw; fresh modifiers default 'normal', DESERIALIZED pre-change scenes default 'none' (saved donuts stay pixel-identical; RNG stream untouched). (3) **ColorRamp ramp widget** — gradient bar + draggable stops in the Shader Editor (src/ui/rampWidget.ts, DOM-free stop math). (4) **Scriptable node API** — `__app.nodes.forMaterial(...)` GraphHandle (add/set/connect/bake/batch), all via GraphEditCommand → undoable, editor reflects next frame. (5) **Bake upgrades** — Generated coords in the bake path (CPU UV-space rasterizer of tracer-identical triGen coords; lazy meshProvider) + per-material Bake Res select (128–1024). (6) **OBJ axis conversion** (Z-up↔Y-up rotations, round-trip identity), **grid z-fight fix** (fractional depth push away from eye; e2e/grid.mjs — SwiftShader shows deterministic bleed, real GPUs dashes), **UV-editor nav e2e**. Also fixed same-day: **workspace dead-space bug** (flexbox grow sums <1 fill only that fraction — normalize sizes on every build; Ray's screenshot). **Parallel-worker lessons:** never run background jobs that `git add -A`/stash while workers are live (raced their WIP twice); shared main.ts got clobbered repeatedly — the final integration audit (tsc + registration grep + the 'blamed' suite on the FINAL tree) caught a disabled-for-isolation line and cleared a phantom cross-worker regression (p14-nodes-b was red only in the mixed WIP tree, green after integration).
- [x] **Intersections shading option (2026-07-10, Ray-requested).** New checkbox in the viewport shading dropdown (between Wireframe and Hidden Line): light grey lines wherever two mesh objects' geometry passes through each other (his repro: plane scaled through the cube), in ALL shading modes incl. wireframe. Core `src/core/mesh/intersect.ts` (Opus worker): Möller '97 tri-tri interval test (epsilon-snapped on-plane verts, coplanar pairs skipped) + `meshIntersectionSegments` with a uniform spatial-hash broad phase; 11 unit tests. Renderer: world-space segments cached per composite key (obj id + gpuMesh version + world matrix), 150 ms throttle (**time-only** — architect fix: the worker's `lines && elapsed` throttle skipped the null-result case, so dragging a heavy non-intersecting mesh re-swept every frame), CPU object-space tri cache, distinct pairs only (self-intersection out of scope). **Rendering is a screen-space RIBBON, not gl.LINES** (architect rework after the eyes-on check): the worker's 1px 0.62-grey hairline passed the pixel rig but was invisible against the matcap greys it sits on (the AO lesson again — screenshots mandatory); now each segment extrudes to ~3px quads in the vertex shader (constant px width at any depth, w-scaled clip offset), zBias 0.004. Look tuned twice on Ray's feedback: full-opacity dark rim read as "grey line on top of a black line"; plain alpha-AA washed out on matcap greys; final = solid 0.72-grey core whose rim fades through a TRANSLUCENT ~35% darkening (blended, depth-write off) — one clean line with contrast on any surface. `segmentsToRibbon` emits planar arrays (VertexArray has no stride/offset). e2e: shading.mjs intersect section (off/on/off block-diff vs core OR rim color, wireframe-mode check, checkbox row). 780 units, shading suite green on SwiftShader AND Vega 7.
- [x] **Use & Refine batch 3 (2026-07-10, workflow `ur3-toolbar-intersect`, 4/4 verified, 0 fix rounds).** (1) **Viewport tool palette** — Blender-style left-edge toolbar in `#viewport-wrap` (`src/ui/toolbar.ts` + registry `ToolDef`), mode-aware via frame-loop `update()`: object = Move/Rotate/Scale/Duplicate/Intersect, edit = Move/Rotate/Scale/Extrude/Inset/Bevel/Loop Cut/Knife/Edge Slide; buttons call the SAME paths as shortcuts (InputManager gained public helpers + `activeOperatorName` for the active-highlight; container pointer-events:none, only buttons interactive). (2) **Intersect tool** — `ops/embedIntersections.ts` (pure, knife-style): selected meshes' edge-piercings vs each other's world-space triangles become real verts + chord-splits, one "Intersect" undo (`intersectCommand.ts`, whole-mesh snapshots); v1 limit documented: faces crossed only through their INTERIOR (big plane through small cube → plane) gain nothing — boolean modifier + interior loops are on the backlog. e2e handle `window.__intersectTool`. (3) **GG slide guides** — generic `Operator.guideSegments?()` hook → gizmoPass `renderGuides()` (near-plane clamp reused); edge slide draws each rail extended −0.5L→+1.5L world-space. (4) **Shift+A submenus** — addMenu root = Mesh ▸ / Light ▸ / Camera with hover flyouts (right-edge flip, 150ms close-grace, click-category fallback keeps old e2e interaction working). Also NEW `NOT-WASTING-CREDITS.md` — standing self-serve backlog (GPU path tracer first) for idle-credit sessions. Integration on the final tree: 789 units, 8 e2e suites incl. donut, shading+edit on real Vega 7, screenshot eyes-on of all four features. Specs tasks/UR3-1..4.md.
- [ ] **Use & Refine (continues after AO).** Ray drives the app for real work; fix what hurts in small verified batches. Punch list (standing + tonight's): (all 2026-07-08 punch items cleared 2026-07-09.)
- [ ] Video production (script, capture, edit) — plan in Notion "Vibe Coding Blender". The fly-through WebM render is a strong beat for the ending.
- [ ] Punch list (seed — grows from usage): theme-picker toggle-close, M→New Collection single undo, lock-to-view framing distance, proportional default radius, sun shadow maps in Rendered mode, knife tool, parenting, .hdr (RGBE) parsing for World, sprinkles lying flat (align-to-normal tuning), UV Editor wheel-zoom/pan e2e coverage.
- [ ] Video production (script, capture, edit) — plan in Notion "Vibe Coding Blender".
